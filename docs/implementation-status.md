# Implementation Status

This file tracks tested implementation slices against `docs/goal.md`.

## Verified Slices

The current codebase has a TypeScript test harness and shared core modules for the runtime behavior that can be reused by the FiveM resource, web/admin panel, sidecars, and SpacetimeDB adapter.

| Phase | Implemented Slice | Evidence |
| --- | --- | --- |
| Phase 01 | Runtime control plane records servers, heartbeats, config, actions, and audit logs. | `src/core/runtime.ts`, `tests/runtime-control-plane.test.ts` |
| Phase 01 | SpacetimeDB server registration now rejects blank server ids, names, environments, and public keys before authoritative server rows are inserted. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-module.test.ts` |
| Phase 01 | Runtime heartbeats can be protected by reusable server identity signing/verification helpers with required nonce/signature fields and per-server nonce replay rejection. | `src/core/runtime.ts`, `tests/runtime-control-plane.test.ts` |
| Phase 01 | Runtime bridge emits server heartbeats through the control plane and signs them with server identity secrets when configured. | `src/runtime/bridge.ts`, `tests/runtime-bridge.test.ts` |
| Phase 01 | SpacetimeDB heartbeat reducers and generated-client calls carry nonce/signature fields, reject missing signatures or replayed heartbeat nonces, and refresh the subscribed server health row transactionally. | `spacetimedb/src/lib.rs`, `src/spacetime/adapter.ts`, `tests/spacetimedb-module.test.ts`, `tests/spacetimedb-generated-client.test.ts` |
| Phase 01 | SpacetimeDB heartbeat writes now reject blank runtime instance ids, server ids, resource versions, FXServer builds, game builds, nonces, and signatures before replay checks or runtime-instance insertion. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-module.test.ts` |
| Phase 01 | Public runtime server, runtime instance, and audit log tables are subscribed into the SpacetimeDB adapter live cache with server-scoped instance/audit views, and audit writes are guarded against unknown server ids. | `spacetimedb/src/lib.rs`, `src/spacetime/adapter.ts`, `tests/spacetimedb-module.test.ts`, `tests/spacetimedb-runtime-state-adapter.test.ts`, `tests/spacetimedb-expanded-adapter.test.ts` |
| Phase 01 | Direct runtime audit writes now validate audit status values locally and in the SpacetimeDB reducer, rejecting malformed lifecycle strings before audit rows become authoritative. | `src/core/runtime.ts`, `spacetimedb/src/lib.rs`, `tests/runtime-control-plane.test.ts`, `tests/spacetimedb-module.test.ts` |
| Phase 01 | SpacetimeDB runtime audit writes now reject blank audit ids, server ids, actor ids, action types, target fields, and before/after JSON before server lookup, status validation, or audit insertion. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-module.test.ts` |
| Phase 01 | Runtime config writes are guarded against unknown servers, acknowledgements track the exact version a server has applied, reject stale or unknown acknowledgements, and persist through SpacetimeDB tables/reducers plus server-scoped live adapter cache. | `src/core/runtime.ts`, `spacetimedb/src/lib.rs`, `src/spacetime/adapter.ts`, `src/spacetime/module_bindings/index.ts`, `tests/runtime-control-plane.test.ts`, `tests/spacetimedb-config-ack.test.ts`, `tests/spacetimedb-config-ack-adapter.test.ts` |
| Phase 01 | SpacetimeDB runtime config writes now reject blank config ids, server ids, namespaces, keys, and value JSON before server lookup or authoritative config row insertion. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-module.test.ts` |
| Phase 01 | SpacetimeDB runtime config acknowledgements now reject blank server ids, namespaces, and keys before config lookup or acknowledgement row writes. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-config-ack.test.ts` |
| Phase 01 | FiveM runtime connector reads admin dashboard config, groups changed rows into trusted `sdb_runtime:syncConfig` events by namespace, tracks applied versions, and suppresses duplicate config syncs. | `src/connectors/fivem-runtime.ts`, `tests/fivem-runtime-connector.test.ts` |
| Phase 01 | FiveM `sdb_runtime` resource scaffold with manifest, server exports, client command, NUI shell, and smoke-covered server event behavior. | `resources/[runtime]/sdb_runtime`, `tests/fivem-resource.test.ts`, `tests/fivem-resource-smoke.test.ts` |
| Phase 01 | FiveM `sdb_runtime` resource keeps a synchronized runtime health cache, exposes it through `GetHealth`, and returns data-only health command payloads with handler names. | `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/fivem-resource.test.ts`, `tests/fivem-resource-smoke.test.ts` |
| Phase 01 | FiveM `sdb_runtime` resource can apply trusted lightweight replicated state-bag hints to `GlobalState` or per-player state while rejecting authoritative state replication. | `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/fivem-resource.test.ts` |
| Phase 01 | Runtime bridge and SpacetimeDB adapter support `HasPermission`, `GetConfig`, local handler registration, action execution, action submission, persisted action completion state, and direct audit writes, with SpacetimeDB action submission guarded against unknown server ids. | `src/core/actions.ts`, `src/core/runtime.ts`, `src/runtime/bridge.ts`, `src/spacetime/adapter.ts`, `spacetimedb/src/lib.rs`, `tests/runtime-control-plane.test.ts`, `tests/runtime-bridge.test.ts`, `tests/spacetimedb-module.test.ts`, `tests/spacetimedb-adapter.test.ts`, `tests/spacetimedb-generated-client.test.ts` |
| Phase 01 | SpacetimeDB runtime action submission now rejects blank action ids, server ids, actor ids, action types, payload hashes, signatures, nonces, and idempotency keys before server lookup, replay checks, or action insertion. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-module.test.ts` |
| Phase 01 | Local runtime action completion is now terminal in the in-memory action bus, matching the SpacetimeDB reducer behavior and preventing late completion attempts from changing final status or writing extra audit rows. | `src/core/actions.ts`, `tests/runtime-control-plane.test.ts` |
| Phase 01 | SpacetimeDB runtime action completion is now terminal, preventing direct reducer calls from changing a completed or failed action's final status after `completed_at` is set. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-module.test.ts` |
| Phase 01 | SpacetimeDB runtime action completion now rejects blank action ids before action lookup or terminal status mutation. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-module.test.ts` |
| Phase 01 | SpacetimeDB runtime action replay checks now scope idempotency keys and nonces to the submitting server, matching the runtime envelope model and preventing one server from blocking another server's action submission. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-module.test.ts` |
| Phase 01 | Runtime bridge completes submitted actions as failed and writes failed audit entries when local handlers throw. | `src/runtime/bridge.ts`, `tests/runtime-bridge.test.ts` |
| Phase 01 | Runtime bridge dispatches post-action hooks after successful local action execution. | `src/runtime/bridge.ts`, `tests/runtime-bridge-hooks.test.ts` |
| Phase 01 | Runtime bridge persists succeeded and failed post-action hook audit records into the authoritative runtime audit log before completing the parent action, without letting failed hooks fail the completed parent action. | `src/runtime/bridge.ts`, `tests/runtime-bridge-hooks.test.ts` |
| Phase 01 | Runtime bridge exposes the current control-plane health snapshot for synchronization into the FiveM resource health cache. | `src/runtime/bridge.ts`, `tests/runtime-bridge.test.ts` |
| Phase 01 | FiveM runtime connector reads admin dashboard health, normalizes it into trusted `sdb_runtime:syncHealth` payloads, and suppresses duplicate health cache updates. | `src/connectors/fivem-runtime.ts`, `tests/fivem-runtime-connector.test.ts` |
| Phase 01 | Runtime command registry maps command names and aliases to typed runtime actions with permission checks, payload schema validation, and audit metadata. | `src/core/commands.ts`, `tests/runtime-command-registry.test.ts` |
| Phase 01 | Runtime command registry construction now rejects blank command names, blank aliases, and duplicate normalized names or aliases before one command can shadow another. | `src/core/commands.ts`, `tests/runtime-command-registry.test.ts` |
| Phase 01 | Runtime bridge dispatches registered commands through the same local handler, action submission, completion, hook, and audit path used by menu actions. | `src/runtime/bridge.ts`, `tests/runtime-bridge.test.ts` |
| Phase 01 | Runtime commands are persisted as live SpacetimeDB control-plane rows with a reducer, generated-client argument ordering, adapter subscriptions, and plugin-scoped cache reads. | `spacetimedb/src/lib.rs`, `src/spacetime/adapter.ts`, `src/spacetime/module_bindings/index.ts`, `tests/spacetimedb-menu-module.test.ts`, `tests/spacetimedb-menu-adapter.test.ts`, `tests/spacetimedb-generated-client.test.ts` |
| Phase 01 | Runtime commands are exposed in admin menu registry reads and can be managed through HTTP routes plus browser panel controls. | `src/admin/service.ts`, `src/admin/http-api.ts`, `admin-panel/`, `tests/admin-menu-write-through.test.ts`, `tests/admin-panel-assets.test.ts` |
| Phase 01 | Runtime UI panels are persisted as live SpacetimeDB control-plane rows with a reducer, generated-client argument ordering, adapter subscriptions, plugin-scoped cache reads, and admin HTTP/browser controls. | `spacetimedb/src/lib.rs`, `src/spacetime/adapter.ts`, `src/spacetime/module_bindings/index.ts`, `src/admin/service.ts`, `src/admin/http-api.ts`, `admin-panel/`, `tests/spacetimedb-menu-module.test.ts`, `tests/spacetimedb-menu-adapter.test.ts`, `tests/spacetimedb-generated-client.test.ts`, `tests/admin-menu-write-through.test.ts`, `tests/admin-panel-assets.test.ts` |
| Phase 01 | SpacetimeDB-shaped adapter boundary for runtime, menus, economy, gameplay, hook subscriptions and reducer calls, with server-scoped runtime config cache updates. | `src/spacetime/adapter.ts`, `tests/spacetimedb-adapter.test.ts`, `tests/spacetimedb-expanded-adapter.test.ts` |
| Phase 01 | Generated TypeScript binding client wrapper for real SpacetimeDB `DbConnection` setup, table subscriptions, row normalization, and reducer argument ordering. | `src/spacetime/adapter.ts`, `tests/spacetimedb-generated-client.test.ts` |
| Phase 01 | Checked-in SpacetimeDB TypeScript bindings cover Rust module public tables, reducers, and a repeatable official codegen command. | `src/spacetime/module_bindings/index.ts`, `scripts/generate-spacetime-bindings.mjs`, `tests/spacetimedb-module-bindings.test.ts` |
| Phase 01 | Rust SpacetimeDB module scaffold with runtime, menus, economy, gameplay, permissions, and hook tables/reducers, including account creation, transactional transfer mutation, inventory stack validation, job grade validation, and permission grant mutation. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-module.test.ts`, `tests/spacetimedb-expanded-module.test.ts`, `tests/spacetimedb-economy-semantics.test.ts`, `tests/spacetimedb-gameplay-semantics.test.ts`, `tests/spacetimedb-menu-module.test.ts`, `cargo check` |
| Phase 01 | Admin service facade for config, plugin, permission, audit dashboard, and deployment-control workflows. | `src/admin/service.ts`, `tests/admin-service.test.ts`, `tests/admin-deployment-service.test.ts` |
| Phase 01 | Admin config, permission, plugin lifecycle, and deployment mutations can write through to SpacetimeDB reducers and HTTP mutation routes await mirrored writes before returning. | `src/admin/service.ts`, `src/admin/http-api.ts`, `src/spacetime/adapter.ts`, `tests/admin-spacetime-write-through.test.ts`, `tests/admin-http-api.test.ts`, `tests/admin-deployment-http-api.test.ts` |
| Phase 01 | Admin economy account creation and transfer routes execute against SpacetimeDB reducers. | `src/admin/service.ts`, `src/admin/http-api.ts`, `tests/admin-economy-write-through.test.ts`, `tests/spacetimedb-economy-adapter.test.ts` |
| Phase 01 | Admin economy deposit, withdrawal, balance adjustment, and transaction void routes execute against SpacetimeDB reducers and are exposed in the browser panel. | `src/admin/service.ts`, `src/admin/http-api.ts`, `admin-panel/`, `tests/admin-economy-write-through.test.ts`, `tests/admin-panel-assets.test.ts` |
| Phase 01 | Admin gameplay primitive registration, inventory grants, and job assignment routes execute against SpacetimeDB reducers. | `src/admin/service.ts`, `src/admin/http-api.ts`, `src/spacetime/adapter.ts`, `tests/admin-gameplay-write-through.test.ts` |
| Phase 01 | Admin menu definition, action, visibility policy, and session routes execute against SpacetimeDB menu reducers. | `src/admin/service.ts`, `src/admin/http-api.ts`, `src/spacetime/adapter.ts`, `tests/admin-menu-write-through.test.ts` |
| Phase 01 | Admin HTTP API and Node server adapter for dashboard, audit search, config, plugin, permission, and deployment routes. | `src/admin/http-api.ts`, `src/admin/http-server.ts`, `tests/admin-http-api.test.ts`, `tests/admin-deployment-http-api.test.ts`, `tests/admin-http-server.test.ts` |
| Phase 01 | Browser admin panel served from `/admin` for dashboard, config, permissions, menus, plugins, deployments, economy, gameplay, and audit search workflows. | `admin-panel/`, `tests/admin-panel-assets.test.ts`, `tests/admin-static-server.test.ts` |
| Phase 01 | Runnable admin connector bootstrap and CLI entrypoint with optional production SpacetimeDB generated-client wiring from environment configuration, including route-level mirrored writes through the configured adapter. | `src/admin/bootstrap.ts`, `src/admin/main.ts`, `tests/admin-bootstrap.test.ts`, `npm run start:admin` smoke test |
| Phase 01 | Admin dashboard and audit search read health, config, plugins, and audit logs from the connected SpacetimeDB live cache when available, preserving local in-memory fallback for development. | `src/admin/service.ts`, `src/spacetime/adapter.ts`, `tests/admin-service.test.ts`, `tests/admin-http-api.test.ts` |
| Phase 01 | Runtime heartbeat health is summarized as online/degraded/offline and exposed through admin dashboard JSON plus the browser panel. | `src/core/runtime.ts`, `src/admin/service.ts`, `admin-panel/`, `tests/runtime-control-plane.test.ts`, `tests/admin-service.test.ts`, `tests/admin-http-api.test.ts`, `tests/admin-bootstrap.test.ts`, `tests/admin-panel-assets.test.ts` |
| Phase 02 | Permission graph with inheritance, expiration, deny override, and assertion API. | `src/core/permissions.ts`, `tests/permission-engine.test.ts` |
| Phase 02 | Mutable permission store for admin edits, principal edges, grants, revocation, audited changes, and engine snapshots. | `src/core/permission-store.ts`, `tests/permission-store.test.ts` |
| Phase 02 | Principals, principal edges, permission grants, permission definitions, server-scoped permission cache versions, and ACE mirror rules are persisted through SpacetimeDB tables/reducers with permission definitions guarded against unknown plugin ids, principal edges guarded against unknown parent/child principals, and grants/ACE mirrors guarded against unknown principals or permission keys, adapter subscriptions and live cache, admin mutation routes, live admin read snapshots, and browser panel controls/read refresh. | `spacetimedb/src/lib.rs`, `src/core/permission-store.ts`, `src/spacetime/adapter.ts`, `src/admin/service.ts`, `src/admin/http-api.ts`, `admin-panel/`, `tests/spacetimedb-permission-control-plane.test.ts`, `tests/spacetimedb-permission-control-plane-adapter.test.ts`, `tests/admin-permission-control-plane-write-through.test.ts`, `tests/admin-panel-assets.test.ts` |
| Phase 02 | SpacetimeDB principal writes now reject blank principal ids, types, external ids, and names before authoritative principal rows are inserted. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-permission-control-plane.test.ts` |
| Phase 02 | SpacetimeDB principal edge writes now reject blank edge ids, parent principal ids, child principal ids, and sources before principal lookup or authoritative edge insertion. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-permission-control-plane.test.ts` |
| Phase 02 | SpacetimeDB principal edge removals now reject blank edge ids before authoritative edge deletion. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-permission-control-plane.test.ts` |
| Phase 02 | SpacetimeDB permission definition writes now reject blank permission ids, keys, descriptions, and plugin ids before plugin lookup or authoritative permission upsert. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-permission-control-plane.test.ts` |
| Phase 02 | Permission grant expiration is accepted at the admin HTTP boundary, stored as a `Date` in the permission engine, mirrored through SpacetimeDB reducer calls, and persisted by the Rust `grant_permission` reducer. | `src/admin/http-api.ts`, `src/admin/service.ts`, `src/spacetime/adapter.ts`, `spacetimedb/src/lib.rs`, `tests/admin-http-api.test.ts`, `tests/admin-spacetime-write-through.test.ts`, `tests/spacetimedb-generated-client.test.ts` |
| Phase 02 | SpacetimeDB permission grants now reject blank grant ids, principal ids, permission keys, and sources before effect validation, principal/permission lookup, or authoritative grant insertion. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-permission-control-plane.test.ts` |
| Phase 02 | SpacetimeDB permission cache acknowledgements now reject unknown server ids, preventing orphaned server-scoped permission cache version rows. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-permission-control-plane.test.ts` |
| Phase 02 | SpacetimeDB permission cache acknowledgements now reject blank server ids before server lookup or authoritative cache acknowledgement upsert. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-permission-control-plane.test.ts` |
| Phase 02 | SpacetimeDB ACE mirror rules now reject blank rule ids, permission keys, and ACE objects before mode validation, permission lookup, or authoritative mirror upsert. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-permission-control-plane.test.ts` |
| Phase 02 | FiveM runtime connector converts live admin permission snapshots into native `sdb_runtime:syncPermissions` cache events, including inherited grants, deny overrides, and duplicate suppression for unchanged principal permission maps. | `src/connectors/fivem-runtime.ts`, `tests/fivem-runtime-connector.test.ts` |
| Phase 02 | Contextual policy constraints for permission decisions, including max amount, required state, namespace scope, menu action payload enforcement, admin editing, SpacetimeDB reducers guarded against unknown permission keys, live adapter cache, and web panel controls. | `src/core/permissions.ts`, `src/core/permission-store.ts`, `src/core/menu.ts`, `src/admin/service.ts`, `src/admin/http-api.ts`, `src/spacetime/adapter.ts`, `spacetimedb/src/lib.rs`, `tests/permission-engine.test.ts`, `tests/permission-store.test.ts`, `tests/menu-action-safety.test.ts`, `tests/admin-policy-constraints-write-through.test.ts`, `tests/spacetimedb-policy-constraints.test.ts`, `tests/spacetimedb-permission-control-plane.test.ts`, `tests/spacetimedb-permission-control-plane-adapter.test.ts` |
| Phase 02 | SpacetimeDB policy constraint writes now reject blank constraint ids, permission keys, constraint types, and constraint JSON before type validation, permission lookup, or authoritative constraint upsert. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-permission-control-plane.test.ts` |
| Phase 02 | SpacetimeDB policy constraint removals now reject blank policy ids before authoritative constraint deletion. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-permission-control-plane.test.ts` |
| Phase 02 | Runtime bridge authorization failures caused by permission or policy denials are written to the authoritative audit log with resolved principals, matched grant details, failed policy results, and denied policy ids before returning to the caller. | `src/core/permissions.ts`, `src/core/menu.ts`, `src/core/commands.ts`, `src/runtime/bridge.ts`, `tests/runtime-bridge.test.ts` |
| Phase 02 | ACE mirror command planner and FiveM resource application path for selected, non-expired permission grants and principal edges. | `src/core/ace.ts`, `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/ace-mirror.test.ts`, `tests/fivem-runtime-connector.test.ts`, `tests/fivem-resource.test.ts`, `tests/fivem-resource-smoke.test.ts` |
| Phase 02 | ACE mirror planning can now use the runtime's injected clock when filtering expired grants and principal edges, keeping temporary Discord/staff mirrors deterministic and aligned with the permission engine. | `src/core/ace.ts`, `tests/ace-mirror.test.ts` |
| Phase 02 | ACE mirror planning now rejects blank, whitespace-bearing, or unknown-mode command tokens before emitting FXServer `add_ace`, `remove_ace`, `add_principal`, or `remove_principal` commands. | `src/core/ace.ts`, `tests/ace-mirror.test.ts` |
| Phase 02 | FiveM runtime connector reads live admin permission snapshots, plans ACE mirror command deltas, emits trusted `sdb_runtime:applyAceMirror` server events, and suppresses duplicate mirror commands once desired state is applied. | `src/connectors/fivem-runtime.ts`, `tests/fivem-runtime-connector.test.ts` |
| Phase 02 | Discord role connector planning and reducer-call application for principal edge sync, including deterministic SpacetimeDB edge ids, optional expiration on Discord-derived role edges, refresh of expired edges when Discord membership still exists, stale edge removal by reducer `edgeId`, and full audit-log reducer payloads for role-sync summaries. | `src/connectors/discord.ts`, `src/spacetime/adapter.ts`, `spacetimedb/src/lib.rs`, `tests/discord-connector.test.ts`, `tests/spacetimedb-generated-client.test.ts` |
| Phase 02 | Discord role connector inputs now fail closed on blank guild ids, role ids, target principals, server ids, member ids, and member role ids before planning any principal or edge reducer writes. | `src/connectors/discord.ts`, `tests/discord-connector.test.ts` |
| Phase 03 | Menu tree filtering hides action-backed entries unless the referenced action row exists, is enabled, and the caller satisfies both menu-level and action-level permissions; server-side action execution still re-checks permissions, validates payloads, enforces confirmation, and emits action audit metadata. | `src/core/menu.ts`, `tests/menu-runtime.test.ts`, `tests/menu-action-safety.test.ts` |
| Phase 03 | Built-in admin menu catalog for player, vehicle, weather/time, teleport, economy, plugin, runtime health, and audit workflows, with dangerous-action confirmation and payload schemas. | `src/core/builtin-admin-menu.ts`, `src/core/menu.ts`, `tests/builtin-admin-menu.test.ts` |
| Phase 03 | Menu tree filtering evaluates enabled visibility policies against caller state, fails closed for missing/disabled/malformed policies, and combines those checks with permission-gated visibility. | `src/core/menu.ts`, `tests/menu-runtime.test.ts` |
| Phase 03 | SpacetimeDB menu definitions, actions, runtime commands, runtime panels, visibility policies, and server-scoped sessions are retained in the adapter live cache, guarded against unknown plugin ids at reducer write time, and exposed through admin menu registry reads plus browser panel edit/session controls and refresh for runtime rebuilds. | `spacetimedb/src/lib.rs`, `src/spacetime/adapter.ts`, `src/admin/service.ts`, `src/admin/http-api.ts`, `admin-panel/`, `tests/spacetimedb-menu-module.test.ts`, `tests/spacetimedb-menu-adapter.test.ts`, `tests/admin-menu-write-through.test.ts`, `tests/admin-panel-assets.test.ts` |
| Phase 03 | SpacetimeDB menu definition writes now reject blank menu ids, plugin ids, and labels before plugin lookup or authoritative menu upsert. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-menu-module.test.ts` |
| Phase 03 | SpacetimeDB menu action writes now reject blank action ids, plugin ids, action types, payload schemas, and audit levels before semantic validation, plugin lookup, or authoritative action upsert. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-menu-module.test.ts` |
| Phase 03 | SpacetimeDB runtime command writes now reject blank command ids, plugin ids, names, action ids, payload schemas, and audit levels before command validation, plugin lookup, or authoritative command upsert. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-menu-module.test.ts` |
| Phase 03 | SpacetimeDB runtime panel writes now reject blank panel ids, plugin ids, titles, and routes before route validation, plugin lookup, or authoritative panel upsert. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-menu-module.test.ts` |
| Phase 03 | SpacetimeDB menu visibility policy writes now reject blank policy ids, plugin ids, and policy JSON before plugin lookup or authoritative policy upsert. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-menu-module.test.ts` |
| Phase 03 | SpacetimeDB menu session open/close reducers now reject blank session ids, server ids, and player ids before session upsert or lookup. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-menu-module.test.ts` |
| Phase 03 | SpacetimeDB live menu cache rows can be converted into a policy-aware `MenuRuntime`, normalizing optional row strings, parsing action payload schemas, and carrying visibility policies into runtime tree filtering/action validation. | `src/spacetime/adapter.ts`, `tests/spacetimedb-menu-adapter.test.ts` |
| Phase 03 | FiveM runtime connector reads admin menu and permission snapshots, builds permission-filtered menu trees through `MenuRuntime`, emits trusted `sdb_runtime:syncMenuTree` updates, and suppresses duplicate tree payloads per principal. | `src/connectors/fivem-runtime.ts`, `tests/fivem-runtime-connector.test.ts` |
| Phase 03 | SpacetimeDB plugin lifecycle reducers now disable plugin-owned menu definitions, menu actions, runtime commands, runtime panels, and visibility policies when a plugin becomes inactive, preventing direct reducer calls from leaving stale menu surfaces visible. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-menu-module.test.ts` |
| Phase 03 | SpacetimeDB menu upsert reducers now reject enabled plugin-owned menu definitions, actions, commands, panels, and visibility policies unless the owning plugin is active, preventing direct reducer calls from reintroducing visible menu surfaces for disabled plugins. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-menu-module.test.ts` |
| Phase 03 | Runtime bridge and FiveM resource deliver permission-filtered menu trees to NUI, request the latest cached tree when the menu opens, render nested menu entries, post selected actions and commands back through the server action path, re-check registered action permissions server-side before local handler dispatch, allow the bridge menu runtime, command registry, and hook runtime to be replaced after live control-plane updates, and refresh their permission engines after live permission changes. | `src/runtime/bridge.ts`, `src/core/menu.ts`, `src/core/commands.ts`, `resources/[runtime]/sdb_runtime`, `tests/runtime-bridge.test.ts`, `tests/runtime-bridge-hooks.test.ts`, `tests/fivem-resource.test.ts`, `tests/fivem-resource-smoke.test.ts` |
| Phase 03 | FiveM `sdb_runtime` now registers first-party admin menu action ids to built-in player, vehicle, world, teleport, and kick dispatchers so synced built-in menu entries execute through the resource's server-side permission-checked action path instead of returning unknown action errors. | `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/fivem-resource.test.ts`, `tests/fivem-resource-smoke.test.ts` |
| Phase 03 | Permission-filtered menu tree nodes now carry action payload schemas and confirmation flags when present, and the FiveM NUI renders schema-derived input fields, validates required payload fields, and prompts for confirmation so built-in admin menu clicks submit typed payloads instead of empty objects while dangerous actions require operator confirmation. | `src/core/menu.ts`, `resources/[runtime]/sdb_runtime/web/app.js`, `resources/[runtime]/sdb_runtime/web/index.html`, `tests/builtin-admin-menu.test.ts`, `tests/fivem-resource.test.ts`, `tests/fivem-runtime-connector.test.ts` |
| Phase 03 | FiveM NUI action feedback now renders readable success and error states instead of raw action-result JSON, and validation failures share the same visible error styling for in-game operators. | `resources/[runtime]/sdb_runtime/web/app.js`, `resources/[runtime]/sdb_runtime/web/index.html`, `tests/fivem-resource.test.ts` |
| Phase 03 | FiveM NUI action forms now render required payload fields followed by optional schema properties, allowing built-in admin actions such as vehicle spawn and teleport to expose optional controls without weakening required-field validation. | `resources/[runtime]/sdb_runtime/web/app.js`, `tests/fivem-resource.test.ts` |
| Phase 03 | Menu payload schemas now support enum-constrained scalar fields, built-in fixed-choice admin actions declare option metadata for weather, economy direction/currency, and plugin status, and the FiveM NUI renders those fields as select controls while backend validation rejects out-of-range values. | `src/core/plugin-data.ts`, `src/core/plugins.ts`, `src/core/builtin-admin-menu.ts`, `resources/[runtime]/sdb_runtime/web/app.js`, `tests/menu-action-safety.test.ts`, `tests/builtin-admin-menu.test.ts`, `tests/fivem-resource.test.ts` |
| Phase 03 | FiveM NUI payload collection now omits blank optional scalar fields instead of coercing empty numbers to zero, and validates numeric input before dispatch so optional controls such as heading do not accidentally alter gameplay actions. | `resources/[runtime]/sdb_runtime/web/app.js`, `tests/fivem-resource.test.ts` |
| Phase 03 | Optional enum-backed NUI fields now start with a blank choice, allowing optional plugin-defined selects to be omitted instead of silently submitting the first allowed option. | `resources/[runtime]/sdb_runtime/web/app.js`, `tests/fivem-resource.test.ts` |
| Phase 03 | FiveM NUI action dispatch now tracks a pending action, disables the submitted action button, ignores duplicate clicks until the server result arrives, and clears the pending state after result rendering. | `resources/[runtime]/sdb_runtime/web/app.js`, `tests/fivem-resource.test.ts` |
| Phase 03 | FiveM NUI action dispatch now handles browser-side callback request failures by rendering an error and clearing the pending action state, preventing a failed NUI request from locking the in-game menu. | `resources/[runtime]/sdb_runtime/web/app.js`, `tests/fivem-resource.test.ts` |
| Phase 03 | FiveM NUI menu refreshes now reapply the pending-action disabled state after replacing the menu tree, preventing live menu updates from re-enabling a submitted action before the server result arrives. | `resources/[runtime]/sdb_runtime/web/app.js`, `tests/fivem-resource.test.ts` |
| Phase 03 | FiveM clients now send local runtime context, including the player's server source and current vehicle network id, to the NUI so matching action payload fields can be prefilled for common built-in admin actions without trusting those defaults server-side. | `resources/[runtime]/sdb_runtime/client/main.lua`, `resources/[runtime]/sdb_runtime/web/app.js`, `tests/fivem-resource.test.ts` |
| Phase 03 | FiveM client runtime context now also includes current player coordinates and heading so teleport and placement-oriented admin action fields can be prefilled by the same NUI context-default mechanism. | `resources/[runtime]/sdb_runtime/client/main.lua`, `resources/[runtime]/sdb_runtime/web/app.js`, `tests/fivem-resource.test.ts` |
| Phase 03 | FiveM client runtime context now prefers active waypoint coordinates for `x` and `y` defaults while retaining current position as fallback, making the built-in Teleport To Marker action populate marker coordinates when a waypoint exists. | `resources/[runtime]/sdb_runtime/client/main.lua`, `resources/[runtime]/sdb_runtime/web/app.js`, `tests/fivem-resource.test.ts` |
| Phase 03 | Waypoint-backed FiveM runtime context now probes terrain height and uses the resolved ground `z` when available, falling back to the player's current altitude when the marker height cannot be resolved. | `resources/[runtime]/sdb_runtime/client/main.lua`, `tests/fivem-resource.test.ts` |
| Phase 03 | FiveM NUI runtime context updates now reapply defaults to already-rendered empty payload fields, allowing late or refreshed context messages to populate action forms without overwriting operator-entered values. | `resources/[runtime]/sdb_runtime/web/app.js`, `tests/fivem-resource.test.ts` |
| Phase 03 | FiveM `sdb_runtime:clientAction` ingress now rejects blank or non-string action names with the same shaped `actionResult` response used by normal dispatch failures, preventing malformed NUI/client events from reaching handler lookup. | `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/fivem-resource-smoke.test.ts`, `tests/fivem-resource.test.ts` |
| Phase 03 | FiveM `sdb_runtime:clientAction` ingress now rejects non-table payloads with a shaped `actionResult` error before handler dispatch, keeping malformed client events out of built-in action handlers. | `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/fivem-resource-smoke.test.ts`, `tests/fivem-resource.test.ts` |
| Phase 03 | FiveM `sdb_runtime:clientAction` dispatch now wraps local handler execution in protected calls and returns shaped failure results when handlers throw, preventing server-side action exceptions from leaving the NUI pending state unresolved. | `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/fivem-resource-smoke.test.ts`, `tests/fivem-resource.test.ts` |
| Phase 03 | FiveM `CallAction` export now protects local handler execution with `pcall`, returning `false` plus the error string instead of throwing through callers when a registered handler fails. | `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/fivem-resource-smoke.test.ts`, `tests/fivem-resource.test.ts` |
| Phase 03 | FiveM `CallAction` export now validates action names and payload shape before handler lookup, returning shaped failures for blank/non-string action ids and non-table payloads even when called directly by another resource. | `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/fivem-resource-smoke.test.ts`, `tests/fivem-resource.test.ts` |
| Phase 03 | Admin control-plane reads can plan active menu refresh targets from live SpacetimeDB menu sessions, expose them over HTTP, and queue/drain refresh targets when plugin lifecycle changes may alter the runtime menu surface. | `src/admin/service.ts`, `src/admin/http-api.ts`, `tests/admin-menu-write-through.test.ts` |
| Phase 03 | `set_runtime_config` menu actions execute as typed control-plane mutations without ad hoc local handlers while preserving action envelopes, permission checks, payload validation, and audit completion. | `src/runtime/bridge.ts`, `tests/runtime-bridge.test.ts` |
| Phase 03 | `open_panel` menu actions execute as typed UI instructions without ad hoc local handlers while preserving authorization, action envelopes, and audit completion. | `src/runtime/bridge.ts`, `tests/runtime-bridge.test.ts` |
| Phase 03 | `execute_server_command` menu actions require an explicit server-command executor and prefix allowlist before dispatch, preserving permission checks, confirmation, action envelopes, and failed audit records for blocked commands. | `src/runtime/bridge.ts`, `tests/runtime-bridge.test.ts` |
| Phase 03 | `execute_server_command` menu action definitions now fail closed unless they declare an explicit required permission, preventing dangerous command actions from producing successful menu audit metadata without an authorization key. | `src/core/menu.ts`, `tests/menu-action-safety.test.ts` |
| Phase 03 | SpacetimeDB `upsert_menu_action` now enforces the same explicit-permission requirement for `execute_server_command` rows, preventing direct reducer calls from inserting dangerous command actions without an authorization key. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-menu-module.test.ts` |
| Phase 03 | `toggle_feature` menu actions mutate authoritative feature flags through runtime config, supporting explicit enabled values and toggle-from-current behavior under the `features` namespace. | `src/runtime/bridge.ts`, `tests/runtime-bridge.test.ts` |
| Phase 03 | `call_reducer` menu actions dispatch through an explicit reducer dispatcher using the action `reducerName`, preserving permission checks, payload validation, action envelopes, and failed audit records when no dispatcher is configured. | `src/runtime/bridge.ts`, `tests/runtime-bridge.test.ts` |
| Phase 03 | `trigger_server_handler` menu actions dispatch through an explicit server handler dispatcher using the action `reducerName` as the handler target, preserving permission checks, payload validation, action envelopes, and failed audit records when no dispatcher is configured. | `src/runtime/bridge.ts`, `tests/runtime-bridge.test.ts` |
| Phase 03 | `trigger_client_event` menu actions dispatch through an explicit client event dispatcher using the action `reducerName` as the event name and the actor principal as the event target, preserving permission checks, payload validation, action envelopes, and failed audit records when no dispatcher is configured. | `src/runtime/bridge.ts`, `tests/runtime-bridge.test.ts` |
| Phase 03 | Built-in admin vehicle repair menu actions use a typed `repair_vehicle` runtime action, dispatch through an explicit vehicle-repair dispatcher, and the FiveM resource applies trusted player-scoped vehicle repairs through a server-only event plus client native repair calls. | `src/core/menu.ts`, `src/core/builtin-admin-menu.ts`, `src/runtime/bridge.ts`, `spacetimedb/src/lib.rs`, `resources/[runtime]/sdb_runtime/server/main.lua`, `resources/[runtime]/sdb_runtime/client/main.lua`, `tests/builtin-admin-menu.test.ts`, `tests/runtime-bridge.test.ts`, `tests/fivem-resource.test.ts`, `tests/spacetimedb-menu-module.test.ts` |
| Phase 03 | Built-in admin vehicle spawn menu actions use a typed `spawn_vehicle` runtime action, dispatch through an explicit vehicle-spawn dispatcher, and preserve action envelopes, permission checks, payload validation, and failed audit records when no dispatcher is configured. | `src/core/menu.ts`, `src/core/builtin-admin-menu.ts`, `src/runtime/bridge.ts`, `tests/builtin-admin-menu.test.ts`, `tests/runtime-bridge.test.ts` |
| Phase 03 | Built-in weather/time menu actions use typed `set_weather` and `set_time` runtime actions, dispatch through an explicit world-state dispatcher, and the FiveM resource applies trusted weather and clock updates through a server-only world-state sync event. | `src/core/menu.ts`, `src/core/builtin-admin-menu.ts`, `src/runtime/bridge.ts`, `resources/[runtime]/sdb_runtime/server/main.lua`, `resources/[runtime]/sdb_runtime/client/main.lua`, `tests/builtin-admin-menu.test.ts`, `tests/runtime-bridge.test.ts`, `tests/fivem-resource.test.ts` |
| Phase 03 | Built-in teleport menu actions use a typed `teleport_player` runtime action, dispatch through an explicit teleport dispatcher, and the FiveM resource applies trusted player-scoped teleports through a server-only event plus client coordinate application. | `src/core/menu.ts`, `src/core/builtin-admin-menu.ts`, `src/runtime/bridge.ts`, `resources/[runtime]/sdb_runtime/server/main.lua`, `resources/[runtime]/sdb_runtime/client/main.lua`, `tests/builtin-admin-menu.test.ts`, `tests/runtime-bridge.test.ts`, `tests/fivem-resource.test.ts` |
| Phase 03 | Built-in player kick menu actions use a typed `kick_player` runtime action, dispatch through an explicit kick dispatcher, and the FiveM resource applies trusted player-scoped kicks through a server-only event. | `src/core/menu.ts`, `src/core/builtin-admin-menu.ts`, `src/runtime/bridge.ts`, `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/builtin-admin-menu.test.ts`, `tests/runtime-bridge.test.ts`, `tests/fivem-resource.test.ts` |
| Phase 03 | FiveM `sdb_runtime` exposes a trusted, player-scoped client event dispatch path for typed menu actions through a validating helper, server-only sync event, and export, rejecting empty event names, non-player targets, and broadcast targets. | `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/fivem-resource.test.ts`, `tests/fivem-resource-smoke.test.ts` |
| Phase 03 | FiveM `sdb_runtime` tracks active NUI menu sessions by player source, removes them when the menu closes, and supports a trusted control-plane refresh event that resends each active session's latest cached permission-filtered menu tree. | `resources/[runtime]/sdb_runtime/server/main.lua`, `resources/[runtime]/sdb_runtime/client/main.lua`, `tests/fivem-resource.test.ts`, `tests/fivem-resource-smoke.test.ts` |
| Phase 03 | FiveM runtime connector drains queued admin menu refresh targets, groups them into one trusted refresh event per affected server, validates control-plane response shape, and avoids emitting refreshes when no sessions are pending. | `src/connectors/fivem-runtime.ts`, `src/connectors/index.ts`, `tests/fivem-runtime-connector.test.ts` |
| Phase 03 | Admin control-plane queues non-authoritative replicated state hints, exposes queue/drain HTTP routes, and the FiveM runtime connector drains them into trusted `sdb_runtime:syncReplicatedState` events grouped by server while stripping control-plane-only fields. | `src/admin/service.ts`, `src/admin/http-api.ts`, `src/connectors/fivem-runtime.ts`, `tests/admin-menu-write-through.test.ts`, `tests/fivem-runtime-connector.test.ts` |
| Phase 03 | Admin control-plane queues weather/time world-state patches, exposes queue/drain HTTP routes, and the FiveM runtime connector drains and merges them into trusted `sdb_runtime:syncWorldState` events grouped by server. | `src/admin/service.ts`, `src/admin/http-api.ts`, `src/connectors/fivem-runtime.ts`, `tests/admin-menu-write-through.test.ts`, `tests/fivem-runtime-connector.test.ts` |
| Phase 03 | FiveM runtime connector can run one full synchronization pass for health, config, native permissions, ACE mirror, menu trees, menu refreshes, replicated state, world state, and queued gameplay actions. | `src/connectors/fivem-runtime.ts`, `tests/fivem-runtime-connector.test.ts` |
| Phase 03 | FiveM runtime connector has a reusable non-overlapping sync loop with failure reporting, optional admin bootstrap wiring, and CLI environment knobs for enabling continuous synchronization. | `src/connectors/fivem-runtime-loop.ts`, `src/admin/bootstrap.ts`, `src/admin/main.ts`, `tests/fivem-runtime-loop.test.ts`, `tests/admin-bootstrap.test.ts` |
| Phase 03 | FiveM sync events have a command/RCON-compatible emitter and the `sdb_runtime` resource normalizes connector payload envelopes through a trusted `sdb_runtime_emit` command ingress. | `src/connectors/fivem-command-emitter.ts`, `resources/[runtime]/sdb_runtime/server/main.lua`, `src/admin/main.ts`, `tests/fivem-command-emitter.test.ts`, `tests/fivem-resource.test.ts`, `tests/admin-bootstrap.test.ts` |
| Phase 03 | FiveM command emission now rejects blank server ids, malformed or non-`sdb_runtime` event names, whitespace-bearing event names, and non-serializable payloads before formatting trusted `sdb_runtime_emit` commands for FXServer execution. | `src/connectors/fivem-command-emitter.ts`, `tests/fivem-command-emitter.test.ts` |
| Phase 03 | FiveM `sdb_runtime_emit` command ingress now mirrors runtime event-name hardening, rejecting missing, non-`sdb_runtime`, whitespace-bearing event names and non-table payloads before dispatching decoded control-plane envelopes. | `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/fivem-resource.test.ts` |
| Phase 03 | FiveM command emission can target an HTTP command endpoint with optional bearer authentication, non-2xx failure reporting, input validation, CLI env wiring, and stdout command fallback for local development. | `src/connectors/fivem-http-command-executor.ts`, `src/admin/main.ts`, `tests/fivem-http-command-executor.test.ts`, `tests/admin-bootstrap.test.ts` |
| Phase 03 | FiveM HTTP command transport now fails closed for arbitrary console commands, only POSTing `sdb_runtime_emit` runtime envelopes to the configured FXServer command endpoint. | `src/connectors/fivem-http-command-executor.ts`, `tests/fivem-http-command-executor.test.ts` |
| Phase 03 | Runtime bridge admin dispatchers can be constructed from the admin control plane, wiring typed vehicle spawn, vehicle repair, world state, teleport, kick, economy balance adjustment, and plugin status menu actions into existing server-scoped queues and control-plane mutations. | `src/runtime/admin-dispatchers.ts`, `tests/runtime-admin-dispatchers.test.ts` |
| Phase 04 | Economy transfer, invoice, cash deposit/withdraw, salary, fines, admin adjustment, item purchase/sale, taxes, business payouts, and transaction void workflows with audit metadata. | `src/core/economy.ts`, `tests/economy-ledger.test.ts`, `tests/economy-invoices.test.ts`, `tests/economy-primitives.test.ts`, `tests/economy-advanced-workflows.test.ts` |
| Phase 04 | Core economy limits can cap action amounts and restrict participating account owner types before ledger mutation or idempotency result storage, preserving balances when a limit blocks an action. | `src/core/economy.ts`, `tests/economy-ledger.test.ts` |
| Phase 04 | Economy limits are persisted as SpacetimeDB control-plane rows with a reducer guarded against unknown permission keys, generated-binding coverage, live adapter cache subscriptions, admin service write-through, and HTTP read/write routes. | `spacetimedb/src/lib.rs`, `src/spacetime/adapter.ts`, `src/spacetime/module_bindings/index.ts`, `src/admin/service.ts`, `src/admin/http-api.ts`, `tests/spacetimedb-economy-semantics.test.ts`, `tests/spacetimedb-economy-adapter.test.ts`, `tests/admin-economy-write-through.test.ts`, `tests/spacetimedb-module-bindings.test.ts` |
| Phase 04 | SpacetimeDB live account and economy-limit cache rows can build an `EconomyLedger` that enforces synced limits, supports documented snake_case limit JSON keys, and treats malformed enabled limits as fail-closed blockers. | `src/spacetime/adapter.ts`, `tests/spacetimedb-economy-adapter.test.ts` |
| Phase 04 | Browser admin panel exposes economy limit creation/update and refresh controls against the tested `/economy/limits` HTTP routes. | `admin-panel/`, `tests/admin-panel-assets.test.ts` |
| Phase 04 | Rust SpacetimeDB economy reducers enforce enabled economy limit rows before ledger mutation, including max amount and allowed account owner type constraints with malformed enabled limits failing closed. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-economy-semantics.test.ts` |
| Phase 04 | SpacetimeDB economy limit writes now reject blank or whitespace-only ids, permission keys, action types, and limit JSON before permission lookup or authoritative limit insertion. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-economy-semantics.test.ts` |
| Phase 04 | Economy ledger and admin routes expose account search, transaction history filters, per-transaction ledger-entry drilldown, browser query-string reads, and live SpacetimeDB adapter cache/read-through for account/transaction/ledger/invoice investigation dashboards. | `src/core/economy.ts`, `src/admin/service.ts`, `src/admin/http-api.ts`, `src/spacetime/adapter.ts`, `admin-panel/`, `tests/economy-ledger.test.ts`, `tests/admin-economy-write-through.test.ts`, `tests/spacetimedb-economy-adapter.test.ts`, `tests/admin-panel-assets.test.ts` |
| Phase 04 | Economy investigation dashboards now support suspicious activity filters and exportable account statements with opening/closing balances, totals, date ranges, admin HTTP routes, browser panel controls, and SpacetimeDB cache-backed read-through. | `src/core/economy.ts`, `src/admin/service.ts`, `src/admin/http-api.ts`, `admin-panel/`, `tests/economy-ledger.test.ts`, `tests/admin-economy-write-through.test.ts`, `tests/admin-panel-assets.test.ts` |
| Phase 04 | Economy idempotency accepts identical retries and rejects reused keys with changed transfer payloads in core and SpacetimeDB reducer semantics. | `src/core/economy.ts`, `spacetimedb/src/lib.rs`, `tests/economy-ledger.test.ts`, `tests/spacetimedb-economy-semantics.test.ts` |
| Phase 04 | SpacetimeDB account transfer helpers now reject blank transaction ids, actor ids, transaction types, source/destination account ids, and idempotency keys before amount validation, idempotency checks, account lookup, or ledger mutation. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-economy-semantics.test.ts` |
| Phase 04 | SpacetimeDB account transfer helpers now write successful audit rows with actor, action type, permission key, account-transfer target, and before/after account balance JSON after ledger-backed transfer mutation. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-economy-semantics.test.ts` |
| Phase 04 | SpacetimeDB single-account transaction helpers now reject blank transaction ids, actor ids, transaction types, account ids, directions, and idempotency keys before amount validation, idempotency checks, account lookup, or ledger mutation. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-economy-semantics.test.ts` |
| Phase 04 | SpacetimeDB single-account transaction helpers now write successful audit rows with actor, action type, permission key, account target, and before/after balance JSON after ledger-backed deposit, withdrawal, or admin adjustment mutation. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-economy-semantics.test.ts` |
| Phase 04 | SpacetimeDB transaction voiding now rejects blank void transaction ids, actor ids, target transaction ids, and idempotency keys before reason validation, idempotency checks, transaction lookup, or ledger reversal. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-economy-semantics.test.ts` |
| Phase 04 | SpacetimeDB transaction voiding now writes successful audit rows with actor, voided transaction target, economy void permission, and before/after account balance JSON after ledger reversal mutation. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-economy-semantics.test.ts` |
| Phase 04 | SpacetimeDB invoice issuance now rejects blank invoice ids, issuer/payer account ids, currencies, issuer actors, and idempotency keys before reason validation, idempotency checks, account lookup, or invoice insertion. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-economy-semantics.test.ts` |
| Phase 04 | SpacetimeDB invoice issuance now writes successful audit rows with actor, invoice target, economy issue permission, empty-before state, and issued invoice payload after invoice persistence. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-economy-semantics.test.ts` |
| Phase 04 | SpacetimeDB invoice payment now rejects blank payment transaction ids, actor ids, invoice ids, and idempotency keys before invoice lookup, idempotency checks, permission checks, or ledger mutation. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-economy-semantics.test.ts` |
| Phase 04 | SpacetimeDB invoice payment now writes successful audit rows with actor, invoice target, economy payment permission, and before/after payer and issuer balance JSON after balanced ledger mutation. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-economy-semantics.test.ts` |
| Phase 04 | SpacetimeDB item economy reducers now reject blank purchased/sold item keys before quantity validation, transfer helper dispatch, ledger metadata generation, or balance mutation. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-economy-semantics.test.ts` |
| Phase 04 | SpacetimeDB economy account creation validates documented owner types before inserting rows, account rows carry created/updated timestamps that refresh on balance mutation, invoice rows carry due dates through issue/payment metadata and adapter caches, invoice payments debit/credit accounts through completed transactions and balanced ledger entries, and reducers expose invoice issue/payment, cash deposit/withdraw, salary, item purchase/sale, fines, tax, business payout, admin adjustment, and transaction void workflows through shared ledger helpers, adapter methods, and generated-client argument ordering. | `spacetimedb/src/lib.rs`, `src/spacetime/adapter.ts`, `src/spacetime/module_bindings/index.ts`, `tests/spacetimedb-expanded-module.test.ts`, `tests/spacetimedb-economy-semantics.test.ts`, `tests/spacetimedb-economy-adapter.test.ts`, `tests/spacetimedb-generated-client.test.ts`, `tests/spacetimedb-module-bindings.test.ts` |
| Phase 04 | SpacetimeDB economy account creation now rejects blank account ids, owner types, owner ids, and currencies before owner-type validation or authoritative account insertion. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-economy-semantics.test.ts` |
| Phase 04 | Plugin economy action API gates invoice, shop, salary, fine, tax, and payout calls through active plugin capabilities, optional runtime-server-scoped deployment checks, amount constraints, and allowed account owner-type constraints before mutating the ledger, failing closed on malformed constraint declarations. | `src/core/plugin-economy-api.ts`, `src/core/economy.ts`, `src/core/plugin-deployment.ts`, `tests/plugin-economy-api.test.ts` |
| Phase 04 | Plugin economy action API now rejects blank plugin ids and actor principal ids before capability lookup or ledger mutation. | `src/core/plugin-economy-api.ts`, `tests/plugin-economy-api.test.ts` |
| Phase 04 | Plugin economy action API now rejects blank optional runtime server scopes before server-scoped capability lookup can run against an invalid server id. | `src/core/plugin-economy-api.ts`, `tests/plugin-economy-api.test.ts` |
| Phase 04 | Plugin economy action API now validates positive integer money amounts before plugin capability lookup or ledger mutation, keeping malformed economy requests from consuming plugin capability checks. | `src/core/plugin-economy-api.ts`, `tests/plugin-economy-api.test.ts` |
| Phase 04 | Plugin economy action API now rejects blank account ids before plugin capability lookup, account owner-type constraint checks, or ledger mutation. | `src/core/plugin-economy-api.ts`, `tests/plugin-economy-api.test.ts` |
| Phase 04 | Plugin economy shop actions now reject blank item keys and non-positive quantities before plugin capability lookup or ledger mutation. | `src/core/plugin-economy-api.ts`, `tests/plugin-economy-api.test.ts` |
| Phase 04 | Built-in economy admin balance adjustment menu actions use a typed `economy_admin_adjust_balance` runtime action with explicit currency and idempotency payload fields, dispatching through an explicit economy adjustment dispatcher instead of a generic reducer action. | `src/core/menu.ts`, `src/core/builtin-admin-menu.ts`, `src/runtime/bridge.ts`, `tests/builtin-admin-menu.test.ts`, `tests/runtime-bridge.test.ts` |
| Phase 04 | Gameplay primitive registry, live SpacetimeDB adapter cache, admin read snapshot, and browser panel refresh for plugin-defined items, jobs, vehicles, locations, inventory grants, character jobs, plugin hooks, and vehicle spawn planning, with SpacetimeDB primitive writes guarded against unknown plugin ids. | `src/core/gameplay.ts`, `src/admin/service.ts`, `src/admin/http-api.ts`, `src/spacetime/adapter.ts`, `spacetimedb/src/lib.rs`, `admin-panel/`, `tests/gameplay-primitives.test.ts`, `tests/spacetimedb-gameplay-semantics.test.ts`, `tests/spacetimedb-gameplay-adapter.test.ts`, `tests/admin-gameplay-write-through.test.ts`, `tests/admin-panel-assets.test.ts` |
| Phase 04 | SpacetimeDB item registration now rejects blank item keys, plugin ids, and labels before plugin lookup or authoritative item insertion. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-gameplay-semantics.test.ts` |
| Phase 04 | SpacetimeDB job registration now rejects blank job keys, plugin ids, labels, and grade JSON before plugin lookup or authoritative job insertion. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-gameplay-semantics.test.ts` |
| Phase 04 | SpacetimeDB vehicle registration now rejects blank models, plugin ids, labels, and categories before plugin lookup or authoritative vehicle insertion. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-gameplay-semantics.test.ts` |
| Phase 04 | SpacetimeDB location registration now rejects blank location keys, plugin ids, and labels before plugin lookup or authoritative location insertion. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-gameplay-semantics.test.ts` |
| Phase 04 | SpacetimeDB inventory grants now reject blank owner ids and item keys before quantity validation, item lookup, or authoritative inventory stack mutation. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-gameplay-semantics.test.ts` |
| Phase 04 | SpacetimeDB job assignments now reject blank character ids, job keys, and grades before job lookup or authoritative character-job mutation. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-gameplay-semantics.test.ts` |
| Phase 04 | Admin gameplay vehicle repair dispatches can be queued and drained over HTTP, grouped by the FiveM runtime connector into trusted `sdb_runtime:repairVehicle` events, and delivered only to explicit player targets with vehicle network ids. | `src/admin/service.ts`, `src/admin/http-api.ts`, `src/connectors/fivem-runtime.ts`, `tests/admin-gameplay-write-through.test.ts`, `tests/fivem-runtime-connector.test.ts` |
| Phase 04 | Admin gameplay vehicle spawn dispatches can be queued and drained over HTTP, grouped by the FiveM runtime connector into trusted `sdb_runtime:spawnVehicles` events, and applied by the single runtime resource through server-only validated vehicle creation. | `src/admin/service.ts`, `src/admin/http-api.ts`, `src/connectors/fivem-runtime.ts`, `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/admin-gameplay-write-through.test.ts`, `tests/fivem-runtime-connector.test.ts`, `tests/fivem-resource.test.ts` |
| Phase 04 | Admin gameplay teleport dispatches can be queued and drained over HTTP, planned by the FiveM runtime connector into trusted `sdb_runtime:teleportPlayer` events, and delivered only to explicit player targets. | `src/admin/service.ts`, `src/admin/http-api.ts`, `src/connectors/fivem-runtime.ts`, `tests/admin-gameplay-write-through.test.ts`, `tests/fivem-runtime-connector.test.ts` |
| Phase 04 | Admin gameplay kick dispatches can be queued and drained over HTTP, planned by the FiveM runtime connector into trusted `sdb_runtime:kickPlayer` events, and delivered only to explicit player targets with required reasons. | `src/admin/service.ts`, `src/admin/http-api.ts`, `src/connectors/fivem-runtime.ts`, `tests/admin-gameplay-write-through.test.ts`, `tests/fivem-runtime-connector.test.ts` |
| Phase 04 | FiveM player-scoped dispatch helpers now normalize target sources through a shared validator, rejecting source `0`, broadcasts, and missing targets before client event dispatch, vehicle repair, vehicle spawn, teleport, or kick actions reach FiveM natives. | `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/fivem-resource.test.ts` |
| Phase 04 | Browser admin panel exposes live gameplay operator controls that queue vehicle spawns, vehicle repairs, teleports, and kicks against the selected server using the tested gameplay dispatch routes. | `admin-panel/`, `tests/admin-panel-assets.test.ts` |
| Phase 05 | Plugin manifest install, enable, disable, uninstall, duplicate primitive identifier validation, dependency/capability validation, dependent cascade disable, active primitive registration, and mirrored dependent status updates. | `src/core/plugins.ts`, `src/admin/service.ts`, `tests/plugin-registry.test.ts`, `tests/plugin-dependencies.test.ts`, `tests/admin-spacetime-write-through.test.ts` |
| Phase 05 | Plugin install, enable, disable, dependent cascade disable, uninstall cascade, and uninstall operations record lifecycle audit events surfaced through the admin dashboard and mirrored to SpacetimeDB audit logs from admin write-through paths. | `src/core/plugins.ts`, `src/admin/service.ts`, `tests/plugin-registry.test.ts`, `tests/plugin-dependencies.test.ts`, `tests/admin-service.test.ts`, `tests/admin-plugin-registry-control-plane.test.ts`, `tests/admin-spacetime-write-through.test.ts` |
| Phase 05 | SpacetimeDB plugin registration now rejects blank plugin id, name, version, trust level, signature, bundle hash, and creator metadata before authoritative plugin rows are inserted. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-registry-control-plane.test.ts` |
| Phase 05 | Plugin marketplace-style packages can install validated manifest metadata with package id, source, publisher, trust level, signature, and manifest hash, preserving package records, lifecycle audit, admin HTTP install, SpacetimeDB manifest/plugin mirroring, and browser panel controls. | `src/core/plugins.ts`, `src/admin/service.ts`, `src/admin/http-api.ts`, `admin-panel/`, `tests/plugin-registry.test.ts`, `tests/admin-plugin-registry-control-plane.test.ts`, `tests/admin-panel-assets.test.ts` |
| Phase 05 | SpacetimeDB package registration now rejects blank package id, plugin id, version, source, publisher, signer, signature, and manifest hash metadata before plugin lookup or authoritative package rows are upserted. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-registry-control-plane.test.ts` |
| Phase 05 | Plugin package installs now verify that the declared manifest hash matches a deterministic canonical hash of the manifest before any plugin or package record is stored. | `src/core/plugins.ts`, `tests/plugin-registry.test.ts`, `tests/admin-plugin-registry-control-plane.test.ts` |
| Phase 05 | Marketplace and community plugin packages now require a configured trusted package signer, verify the package signature over the manifest hash, and retain signer provenance in package records. | `src/core/plugins.ts`, `tests/plugin-registry.test.ts`, `tests/admin-plugin-registry-control-plane.test.ts` |
| Phase 05 | Admin connector bootstrap and the browser package form now wire trusted package signer ids through marketplace/community package installation, with a CLI env knob for package signer secrets. | `src/admin/bootstrap.ts`, `src/admin/main.ts`, `admin-panel/`, `tests/admin-bootstrap.test.ts`, `tests/admin-panel-assets.test.ts` |
| Phase 05 | Package signer revocation now blocks future marketplace/community package installs, prevents revoked-signer packages from being re-enabled, disables currently installed package-backed plugins from that signer, mirrors disabled status through admin HTTP write-through, and exposes browser controls. | `src/core/plugins.ts`, `src/admin/service.ts`, `src/admin/http-api.ts`, `admin-panel/`, `tests/plugin-registry.test.ts`, `tests/admin-plugin-registry-control-plane.test.ts`, `tests/admin-panel-assets.test.ts` |
| Phase 05 | Package signer revocation now records an explicit `plugin.package_signer_revoked` audit event with the affected plugin ids, including revocations that do not disable any installed package, and admin write-through mirrors that audit row to SpacetimeDB. | `src/core/plugins.ts`, `tests/plugin-registry.test.ts`, `tests/admin-plugin-registry-control-plane.test.ts` |
| Phase 05 | Package signer revocation now requires and preserves operator actor/reason context through local registry audit events, dependency cascade metadata, admin HTTP, browser controls, and SpacetimeDB revocation write-through. | `src/core/plugins.ts`, `src/admin/service.ts`, `src/admin/http-api.ts`, `admin-panel/`, `tests/plugin-registry.test.ts`, `tests/plugin-dependencies.test.ts`, `tests/admin-plugin-registry-control-plane.test.ts`, `tests/admin-panel-assets.test.ts` |
| Phase 05 | Local package signer revocation now rejects blank signer ids before signer lookup or audit writes, matching the SpacetimeDB reducer's required signer-id guard. | `src/core/plugins.ts`, `tests/plugin-registry.test.ts` |
| Phase 05 | Package signer revocation now cascades through plugin dependency and capability relationships, removing dependent runtime surfaces and including all newly disabled plugins in the signer-revocation audit metadata. | `src/core/plugins.ts`, `tests/plugin-dependencies.test.ts`, `tests/plugin-registry.test.ts` |
| Phase 05 | Plugin package provenance is persisted as SpacetimeDB `plugin_packages` rows with reducer write-through, generated binding coverage, adapter live-cache subscription, and admin registry reads from the authoritative cache. | `spacetimedb/src/lib.rs`, `src/spacetime/adapter.ts`, `src/spacetime/module_bindings/index.ts`, `src/admin/service.ts`, `tests/spacetimedb-plugin-registry-control-plane.test.ts`, `tests/spacetimedb-plugin-registry-adapter.test.ts`, `tests/admin-plugin-registry-control-plane.test.ts`, `tests/spacetimedb-module-bindings.test.ts` |
| Phase 05 | Package signer revocations are persisted as SpacetimeDB `plugin_package_signer_revocations` rows with reducer write-through, generated binding coverage, and adapter live-cache subscription. | `spacetimedb/src/lib.rs`, `src/spacetime/adapter.ts`, `src/spacetime/module_bindings/index.ts`, `src/admin/service.ts`, `tests/spacetimedb-plugin-registry-control-plane.test.ts`, `tests/spacetimedb-plugin-registry-adapter.test.ts`, `tests/admin-plugin-registry-control-plane.test.ts`, `tests/spacetimedb-module-bindings.test.ts` |
| Phase 05 | SpacetimeDB package signer revocation rows now include `affected_plugin_ids_json`, populated from the reducer's actual disabled package-backed plugin ids and exposed through the adapter cache and generated bindings. | `spacetimedb/src/lib.rs`, `src/spacetime/adapter.ts`, `src/spacetime/module_bindings/index.ts`, `tests/spacetimedb-plugin-registry-control-plane.test.ts`, `tests/spacetimedb-plugin-registry-adapter.test.ts`, `tests/admin-plugin-registry-control-plane.test.ts` |
| Phase 05 | SpacetimeDB package signer revocation now rejects blank signer ids, actors, and reasons before disabling plugins, revoking signer-owned bundles, or writing revocation rows. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-registry-control-plane.test.ts` |
| Phase 05 | Admin plugin registry snapshots expose SpacetimeDB package signer revocations and package installs fail closed when the signer is already revoked in the authoritative cache. | `src/admin/service.ts`, `tests/admin-plugin-registry-control-plane.test.ts` |
| Phase 05 | SpacetimeDB package registration rejects writes from package signers already recorded as revoked, preventing alternate clients from bypassing admin-layer package trust enforcement. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-registry-control-plane.test.ts` |
| Phase 05 | SpacetimeDB package signer revocation now disables existing package-backed plugin rows for that signer inside the reducer, so direct control-plane revocations have the same plugin-status side effect as admin HTTP revocations. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-registry-control-plane.test.ts` |
| Phase 05 | SpacetimeDB package signer revocation is idempotent and update-safe, replacing existing revocation metadata by signer id instead of failing duplicate retry/reconcile calls. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-registry-control-plane.test.ts` |
| Phase 05 | Plugin registry snapshots expose installed package provenance alongside plugin, manifest, runtime instance, and config rows, including local package records when the admin service is backed by SpacetimeDB cache reads. | `src/admin/service.ts`, `tests/admin-plugin-registry-control-plane.test.ts` |
| Phase 05 | Plugin manifests, runtime instances, config values, live plugin listing, full registry snapshots, and uninstall lifecycle are persisted through SpacetimeDB reducers with runtime/config writes guarded against unknown plugin ids, live adapter cache with server-scoped runtime/config rows, admin routes, and browser panel controls/read refresh. | `src/core/plugins.ts`, `spacetimedb/src/lib.rs`, `src/spacetime/adapter.ts`, `src/admin/service.ts`, `src/admin/http-api.ts`, `admin-panel/`, `tests/spacetimedb-plugin-registry-control-plane.test.ts`, `tests/spacetimedb-plugin-registry-adapter.test.ts`, `tests/admin-plugin-registry-control-plane.test.ts`, `tests/admin-panel-assets.test.ts` |
| Phase 05 | Plugin config schemas from manifests are exposed for active plugins and admin config writes are validated against those schemas before SpacetimeDB write-through. | `src/core/plugins.ts`, `src/admin/service.ts`, `tests/plugin-registry.test.ts`, `tests/admin-plugin-registry-control-plane.test.ts` |
| Phase 05 | Plugin manifests expose active gameplay primitives for runtime registration. | `src/core/plugins.ts`, `tests/plugin-gameplay-manifest.test.ts` |
| Phase 05 | Plugin manifests expose active runtime command primitives, and disabling a plugin or dependency removes those commands from the active runtime surface. | `src/core/plugins.ts`, `tests/plugin-registry.test.ts`, `tests/plugin-dependencies.test.ts` |
| Phase 05 | Plugin manifests expose active UI panel primitives, and disabling a plugin or dependency removes those panels from the active runtime surface. | `src/core/plugins.ts`, `tests/plugin-registry.test.ts`, `tests/plugin-dependencies.test.ts` |
| Phase 05 | FiveM compatibility server-command manifest entries now require declared permissions at install time, preserving the permission-gated runtime action model for legacy command mappings. | `src/core/plugins.ts`, `tests/plugin-registry.test.ts` |
| Phase 05 | SpacetimeDB manifest registration now validates manifest JSON object shape, rejects menu, command, panel, FiveM `serverCommands`, and FiveM export permission references unless they are declared by the manifest, and rejects FiveM command/export entries without required actions, preventing direct reducer writes from bypassing registry manifest validation. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-registry-control-plane.test.ts` |
| Phase 05 | Built-in plugin status menu actions use a typed `set_plugin_status` runtime action, dispatch through an explicit plugin status dispatcher, and SpacetimeDB menu action validation accepts first-party typed runtime action names. | `src/core/menu.ts`, `src/core/builtin-admin-menu.ts`, `src/runtime/bridge.ts`, `spacetimedb/src/lib.rs`, `tests/builtin-admin-menu.test.ts`, `tests/runtime-bridge.test.ts`, `tests/spacetimedb-menu-module.test.ts` |
| Phase 05 | Plugin manifests understand FiveM compatibility metadata for dependencies, files, NUI pages, exports, server commands, and `provide` shims, including permission validation and dependent disable cascades. | `src/core/plugins.ts`, `tests/plugin-registry.test.ts`, `tests/plugin-dependencies.test.ts` |
| Phase 05 | FiveM compatibility dependencies can now be satisfied by active plugin `provide` shims, and disabling a provider cascades to dependent plugins that relied on the provided legacy resource name. | `src/core/plugins.ts`, `tests/plugin-registry.test.ts`, `tests/plugin-dependencies.test.ts` |
| Phase 05 | QBCore compatibility now has a real `qb-core` facade resource that provides `qb-core`, depends on `sdb_runtime`, exports `GetCoreObject`, and delegates direct server `GetPlayer` plus client `GetPlayerData` calls into runtime-owned QBCore-shaped player data exports. | `resources/[compat]/qb-core`, `resources/[runtime]/sdb_runtime`, `tests/qbcore-compat-resource.test.ts` |
| Phase 05 | QBCore compatibility now keeps runtime-owned online player session state keyed by FiveM source, exposes direct and all-player registry lookups, caches client `PlayerData`, and emits QBCore loaded, unloaded, and player data update events through the compatibility facade. | `resources/[compat]/qb-core`, `resources/[runtime]/sdb_runtime`, `tests/qbcore-compat-resource.test.ts` |
| Phase 05 | QBCore PlayerData compatibility now has a trusted server-only sync ingress for authoritative PlayerData snapshots, updating runtime player cache rows and emitting QBCore money, job, gang, and metadata delta events when synced snapshots change. | `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/qbcore-compat-resource.test.ts` |
| Phase 05 | FiveM runtime connector full sync passes now include QBCore PlayerData snapshot emission, reading server-scoped snapshots from the admin plane, grouping them into `sdb_runtime:syncQbPlayerData` events, validating/defaulting QBCore-shaped payload fields, and suppressing duplicate emissions. | `src/connectors/fivem-runtime.ts`, `tests/fivem-runtime-connector.test.ts` |
| Phase 05 | Admin QBCore PlayerData snapshots are now built from SpacetimeDB-backed online menu sessions, player principals, character economy accounts, job assignments, job definitions, inventory stacks, and item definitions, then served through `/qbcore/player-data` for the FiveM runtime connector. | `src/admin/service.ts`, `src/admin/http-api.ts`, `tests/admin-gameplay-write-through.test.ts` |
| Phase 05 | QBCore character identity now has first-class SpacetimeDB `characters` rows and adapter cache support, with selected character rows feeding `/qbcore/player-data` citizen id, cid, charinfo, metadata, position, license, money, job, and inventory owner resolution. | `spacetimedb/src/lib.rs`, `src/spacetime/adapter.ts`, `src/admin/service.ts`, `tests/spacetimedb-gameplay-adapter.test.ts`, `tests/admin-gameplay-write-through.test.ts` |
| Phase 05 | QBCore character rows can now be upserted and selected through admin service and HTTP write-through paths, backed by SpacetimeDB `upsert_character`; selecting a character clears other selected rows for the same player principal inside the reducer. | `src/admin/service.ts`, `src/admin/http-api.ts`, `spacetimedb/src/lib.rs`, `tests/admin-gameplay-write-through.test.ts`, `tests/spacetimedb-gameplay-semantics.test.ts` |
| Phase 05 | Runtime-originated QBCore character persistence now has a queue surface in the FiveM resource, `characterId` propagation in synced PlayerData, queued updates from `UpdatePlayerData`, `SetMetaData`, and `SetJobDuty`, and an admin `/qbcore/character-updates` ingest route that writes those payloads through to SpacetimeDB `upsert_character`. | `resources/[runtime]/sdb_runtime/server/main.lua`, `src/admin/service.ts`, `src/admin/http-api.ts`, `src/connectors/fivem-runtime.ts`, `tests/qbcore-compat-resource.test.ts`, `tests/admin-gameplay-write-through.test.ts`, `tests/fivem-runtime-connector.test.ts` |
| Phase 05 | FiveM `sdb_runtime` now rejects whitespace-only QBCore character ids and citizen ids before queueing character persistence or character-selection payloads. | `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/qbcore-compat-resource.test.ts` |
| Phase 05 | FiveM runtime sync passes can now drain queued QBCore character updates from a configured runtime client and POST them to `/qbcore/character-updates` before pushing the next PlayerData snapshot, with a concrete HTTP runtime export bridge client for `DrainQbCharacterUpdates`. | `src/connectors/fivem-runtime.ts`, `src/connectors/fivem-http-runtime-client.ts`, `src/admin/bootstrap.ts`, `tests/fivem-runtime-connector.test.ts`, `tests/fivem-http-runtime-client.test.ts`, `tests/admin-bootstrap.test.ts` |
| Phase 05 | The HTTP runtime export bridge client now rejects blank server ids before POSTing drain requests for QBCore character, money, inventory, or selection queues. | `src/connectors/fivem-http-runtime-client.ts`, `tests/fivem-http-runtime-client.test.ts` |
| Phase 05 | The HTTP runtime export bridge client now reports invalid JSON responses as stable bridge errors before queue-drain results are interpreted. | `src/connectors/fivem-http-runtime-client.ts`, `tests/fivem-http-runtime-client.test.ts` |
| Phase 05 | QBCore `AddMoney`, `RemoveMoney`, and `SetMoney` now queue runtime money updates that the FiveM connector drains and persists through `/qbcore/money-updates` as ledger-backed SpacetimeDB `admin_adjust_balance` calls against active character economy accounts. | `resources/[runtime]/sdb_runtime/server/main.lua`, `src/admin/service.ts`, `src/admin/http-api.ts`, `src/connectors/fivem-runtime.ts`, `src/connectors/fivem-http-runtime-client.ts`, `tests/qbcore-compat-resource.test.ts`, `tests/admin-economy-write-through.test.ts`, `tests/fivem-runtime-connector.test.ts`, `tests/fivem-http-runtime-client.test.ts` |
| Phase 05 | FiveM runtime queue drains now reject whitespace-only QBCore identifiers before posting money, inventory, character selection, or character update payloads into the admin control plane. | `src/connectors/fivem-runtime.ts`, `tests/fivem-runtime-connector.test.ts` |
| Phase 05 | FiveM `sdb_runtime` now rejects whitespace-only QBCore money types, item keys, and resolved character ids before queueing runtime-originated money or inventory persistence payloads. | `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/qbcore-compat-resource.test.ts` |
| Phase 05 | QBCore `AddItem`, `RemoveItem`, `GetItemByName`, and `HasItem` now queue runtime inventory updates that the FiveM connector drains and persists through `/qbcore/inventory-updates` as SpacetimeDB `grant_item` and `remove_item` gameplay mutations. | `resources/[runtime]/sdb_runtime/server/main.lua`, `spacetimedb/src/lib.rs`, `src/admin/service.ts`, `src/admin/http-api.ts`, `src/spacetime/adapter.ts`, `src/connectors/fivem-runtime.ts`, `src/connectors/fivem-http-runtime-client.ts`, `tests/qbcore-compat-resource.test.ts`, `tests/admin-gameplay-write-through.test.ts`, `tests/fivem-runtime-connector.test.ts`, `tests/fivem-http-runtime-client.test.ts`, `tests/spacetimedb-gameplay-semantics.test.ts` |
| Phase 05 | QBCore `SetGang` now queues runtime character updates with `gangJson`, persists gang state on SpacetimeDB character rows, and hydrates `/qbcore/player-data` gang payloads from authoritative character data. | `resources/[runtime]/sdb_runtime/server/main.lua`, `spacetimedb/src/lib.rs`, `src/admin/service.ts`, `src/admin/http-api.ts`, `src/spacetime/adapter.ts`, `src/connectors/fivem-runtime.ts`, `tests/qbcore-compat-resource.test.ts`, `tests/admin-gameplay-write-through.test.ts`, `tests/spacetimedb-gameplay-semantics.test.ts` |
| Phase 05 | QBCore `UpdatePlayerData` and `Save` now refresh the player's server-side coordinates and heading before queueing character updates, preserving current last-position data through the existing SpacetimeDB character persistence path. | `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/qbcore-compat-resource.test.ts` |
| Phase 05 | QBCore `Logout` now refreshes position, queues character persistence, emits the player unload event, and removes the player from the runtime QBCore session cache. | `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/qbcore-compat-resource.test.ts` |
| Phase 05 | QBCore runtime character selection now flows from FiveM through `SelectQbCharacter`, a drainable runtime selection queue, the HTTP runtime bridge, the FiveM connector, and `/qbcore/character-selections` into the authoritative `characters.selected` model. | `resources/[runtime]/sdb_runtime/server/main.lua`, `src/admin/service.ts`, `src/admin/http-api.ts`, `src/connectors/fivem-runtime.ts`, `src/connectors/fivem-http-runtime-client.ts`, `tests/qbcore-compat-resource.test.ts`, `tests/admin-gameplay-write-through.test.ts`, `tests/fivem-runtime-connector.test.ts`, `tests/fivem-http-runtime-client.test.ts` |
| Phase 05 | QBCore players now expose local extension helpers for `SetPlayerData`, `GetMetaData`, `AddMethod`, and `AddField`, with `SetPlayerData` queueing character persistence and emitting the standard player data update event. | `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/qbcore-compat-resource.test.ts` |
| Phase 05 | FiveM `sdb_runtime` QBCore player mutators now reject whitespace-only PlayerData keys, metadata keys, job names, gang names, dynamic method names, and dynamic field names before mutating cached PlayerData. | `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/qbcore-compat-resource.test.ts` |
| Phase 05 | QBCore `SetJob` now updates runtime PlayerData, emits job and player-data update events, and queues character persistence from the runtime facade. | `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/qbcore-compat-resource.test.ts` |
| Phase 05 | QBCore server lookup and duty helpers now resolve identifiers, sources, citizen ids, phone numbers, accounts, charinfo fields, on-duty players, and duty counts through the compatibility facade and runtime-owned QBCore session cache. | `resources/[compat]/qb-core/server/main.lua`, `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/qbcore-compat-resource.test.ts` |
| Phase 05 | QBCore server world, routing, opt-in, notification, logging, and kick helpers now exist in the `qb-core` facade, using FiveM natives for coordinate/routing helpers and delegating kicks through `sdb_runtime`. | `resources/[compat]/qb-core/server/main.lua`, `tests/qbcore-compat-resource.test.ts`, `tests/qbcore-common-resource-smoke.test.ts` |
| Phase 05 | QBCore server vehicle creation helpers `SpawnVehicle`, `CreateAutomobile`, and `CreateVehicle` now preserve documented server call signatures while delegating actual creation through `sdb_runtime:SpawnVehicles`. | `resources/[compat]/qb-core/server/main.lua`, `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/qbcore-compat-resource.test.ts`, `tests/qbcore-common-resource-smoke.test.ts` |
| Phase 05 | FiveM `sdb_runtime` QBCore lookup helpers now reject whitespace-only source, citizen id, phone, account, and charinfo lookup strings before matching cached player session data. | `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/qbcore-compat-resource.test.ts` |
| Phase 05 | FiveM `sdb_runtime` QBCore duty helpers now reject whitespace-only job names before scanning cached player session data for on-duty players. | `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/qbcore-compat-resource.test.ts` |
| Phase 05 | QBCore callback compatibility now supports `CreateCallback`, client `TriggerCallback`, server `TriggerClientCallback`, client callback registration, pending callback tables, promise-style awaits, and standard QBCore callback response events through the `qb-core` facade. | `resources/[compat]/qb-core/server/main.lua`, `resources/[compat]/qb-core/client/main.lua`, `tests/qbcore-compat-resource.test.ts` |
| Phase 05 | QBCore command and permission compatibility now exposes `HasPermission`, `GetPermission`, `AddPermission`, `RemovePermission`, `QBCore.Commands.Add`, and `QBCore.Commands.Refresh`, backed by the runtime permission cache and ACE compatibility commands. | `resources/[compat]/qb-core/server/main.lua`, `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/qbcore-compat-resource.test.ts` |
| Phase 05 | QBCore client helper compatibility now exposes PlayerData, notify, progressbar, item checks, vehicle pool access, coordinates, nearby player/ped/object/vehicle helpers, vehicle spawn/delete requests, plate lookup, and vehicle property get/set helpers in the `qb-core` facade. | `resources/[compat]/qb-core/client/main.lua`, `tests/qbcore-compat-resource.test.ts`, `tests/qbcore-common-resource-smoke.test.ts` |
| Phase 05 | QBCore client vehicle helper requests now bridge through server events into `sdb_runtime` server-side vehicle spawn and delete exports, keeping vehicle creation/deletion out of client-authoritative state. | `resources/[compat]/qb-core/server/main.lua`, `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/qbcore-compat-resource.test.ts` |
| Phase 05 | QBCore shared data compatibility now maps authoritative gameplay item, job, and vehicle primitives into `QBCore.Shared` registries, syncs them to the FiveM runtime as `sdb_runtime:syncQbShared`, and exposes the live shared table through server and client `qb-core` facades. | `src/connectors/fivem-runtime.ts`, `resources/[runtime]/sdb_runtime`, `resources/[compat]/qb-core`, `tests/fivem-runtime-connector.test.ts`, `tests/qbcore-compat-resource.test.ts` |
| Phase 05 | QBCore common resource smoke coverage now gates representative legacy resource patterns, including core object access, player lookup, money/job methods, callbacks, commands, usable items, client notify/item/proximity helpers, shared data, and `qb-core` provide/dependency behavior. | `tests/qbcore-common-resource-smoke.test.ts`, `tests/qbcore-compat-resource.test.ts`, `tests/plugin-dependencies.test.ts` |
| Phase 05 | QBCore usable item compatibility now supports server `CreateUseableItem`, `CanUseItem`, `HasItem`, `UseItem`, and `QBCore:Server:UseItem` callbacks through the `qb-core` facade, guarded by runtime player inventory lookup before invoking callbacks. | `resources/[compat]/qb-core/server/main.lua`, `resources/[test]/sdb_runtime_smoke/server/main.lua`, `tests/qbcore-compat-resource.test.ts`, `tests/qbcore-common-resource-smoke.test.ts` |
| Phase 05 | QBCore migration tooling can now convert legacy QBCore shared/player exports into authoritative runtime primitives and admin write-through operations for items, jobs, vehicles, gang extension entities, character rows, economy accounts, and job assignments, with malformed keys and invalid balances rejected before partial writes. | `src/core/qbcore-migration.ts`, `tests/qbcore-migration.test.ts` |
| Phase 05 | Plugin manifests can declare approved namespaced schema migrations, expose those declarations to the registry, validate malformed declarations, and apply approved schemas through SpacetimeDB during install. | `src/core/plugins.ts`, `src/admin/service.ts`, `tests/plugin-registry.test.ts`, `tests/admin-plugin-registry-control-plane.test.ts` |
| Phase 05 | Plugin hook declarations, hook capability checks, priority dispatch, payload validation, hook audit output, and SpacetimeDB hook registration guarded against unknown plugin ids. | `src/core/hooks.ts`, `src/core/plugins.ts`, `spacetimedb/src/lib.rs`, `tests/hook-runtime.test.ts`, `tests/plugin-hook-manifest.test.ts`, `tests/spacetimedb-gameplay-semantics.test.ts` |
| Phase 05 | SpacetimeDB hook registration now rejects blank hook ids, plugin ids, hook names, capabilities, handler types, and handler refs before plugin lookup, handler validation, or authoritative hook insertion. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-gameplay-semantics.test.ts` |
| Phase 05 | Plugin hook dispatch records failed hook audit entries for handler crashes and payload schema mismatches, then continues lower-priority hooks to preserve full audit visibility without one plugin blocking the hook stream. | `src/core/hooks.ts`, `tests/hook-runtime.test.ts` |
| Phase 05 | Namespaced plugin schema/entity storage with validation. | `src/core/plugin-data.ts`, `tests/plugin-data-store.test.ts` |
| Phase 05 | Plugin schema and namespaced entity control-plane persistence through SpacetimeDB reducers, live adapter cache, admin mutation/read routes with plugin/entity filters, subscriptions, and admin panel controls/search. | `spacetimedb/src/lib.rs`, `src/spacetime/adapter.ts`, `src/admin/service.ts`, `src/admin/http-api.ts`, `admin-panel/`, `tests/spacetimedb-plugin-data-module.test.ts`, `tests/spacetimedb-plugin-data-adapter.test.ts`, `tests/admin-plugin-data-write-through.test.ts`, `tests/admin-panel-assets.test.ts` |
| Phase 06 | Signed bundle verification, duplicate capability-key validation, capability checks, approval workflow, kill switch, signer revocation, failed deployment blocking, rollback with restored bundle/version audit metadata, and deployment audit. | `src/core/plugin-deployment.ts`, `tests/plugin-deployment.test.ts`, `tests/plugin-lifecycle-controls.test.ts` |
| Phase 06 | Plugin bundle ids are now immutable within the deployment manager, rejecting duplicate bundle registration before metadata can replace a previously reviewed signed bundle record. | `src/core/plugin-deployment.ts`, `tests/plugin-deployment.test.ts` |
| Phase 06 | Plugin bundle registration now rejects blank id, plugin id, version, artifact URL, hash, signature, and signer metadata locally before signed bundle records are stored. | `src/core/plugin-deployment.ts`, `tests/plugin-deployment.test.ts` |
| Phase 06 | Plugin bundle registration now validates runtime type strings at runtime before local storage, matching the SpacetimeDB reducer's allowed `wasm`, `js_sidecar`, and `native_sidecar` bundle types. | `src/core/plugin-deployment.ts`, `tests/plugin-deployment.test.ts` |
| Phase 06 | Plugin bundle registration now rejects blank capability keys before signed bundle metadata is stored locally, matching reducer-side capability key requirements. | `src/core/plugin-deployment.ts`, `tests/plugin-deployment.test.ts` |
| Phase 06 | Plugin bundle registration now rejects forbidden direct database sandbox capabilities before signed bundle metadata is stored locally, matching reducer-side capability ingress policy. | `src/core/plugin-deployment.ts`, `tests/plugin-deployment.test.ts` |
| Phase 06 | Plugin bundle registration now rejects present capability constraints unless they are JSON-object-shaped metadata, matching SpacetimeDB capability row ingress and preventing arrays or scalar values from being treated as unconstrained bundles. | `src/core/plugin-deployment.ts`, `tests/plugin-deployment.test.ts` |
| Phase 06 | Plugin bundle registration now validates known capability constraint fields, rejecting malformed payload limits and actor principal allow-lists before local signed bundle metadata can reach runtime dispatch. | `src/core/plugin-deployment.ts`, `tests/plugin-deployment.test.ts`, `tests/plugin-action-broker.test.ts` |
| Phase 06 | Plugin bundle registration and SpacetimeDB capability writes now validate economy capability constraints such as max amount, allowed account owner types, and on-duty requirements before malformed signed metadata can reach ledger dispatch. | `src/core/plugin-deployment.ts`, `spacetimedb/src/lib.rs`, `tests/plugin-deployment.test.ts`, `tests/plugin-economy-api.test.ts`, `tests/spacetimedb-plugin-bundles.test.ts` |
| Phase 06 | Signed bundle registration and sidecar action dispatch now reject blank or whitespace-bearing payload-limit field names before malformed capability constraints can become signed metadata or runtime action bounds. | `src/core/plugin-deployment.ts`, `src/core/plugin-action-broker.ts`, `tests/plugin-deployment.test.ts`, `tests/plugin-action-broker.test.ts` |
| Phase 06 | SpacetimeDB bundle registration now rejects blank bundle ids before signed bundle metadata can be inserted as an authoritative row. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-bundles.test.ts` |
| Phase 06 | SpacetimeDB bundle registration now rejects blank plugin ids explicitly before plugin lookup, keeping signed bundle metadata validation aligned with local registration. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-bundles.test.ts` |
| Phase 06 | SpacetimeDB `register_plugin_bundle` now rejects duplicate bundle ids instead of updating signed bundle metadata, preserving immutable bundle provenance for all control-plane clients. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-bundles.test.ts` |
| Phase 06 | SpacetimeDB plugin capability declarations are insert-only by id, preventing clients from rewriting signed bundle capability constraints after registration. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-bundles.test.ts` |
| Phase 06 | SpacetimeDB plugin capability declarations now reject blank capability row ids before signed capability metadata can be inserted. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-bundles.test.ts` |
| Phase 06 | SpacetimeDB plugin capability declarations now reject blank plugin and bundle ids before ownership lookup, keeping capability metadata tied to explicit bundle ownership. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-bundles.test.ts` |
| Phase 06 | SpacetimeDB plugin capability declarations now reject duplicate capability keys within the same bundle, matching signed bundle validation and preventing duplicate capability rows under alternate ids. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-bundles.test.ts` |
| Phase 06 | SpacetimeDB plugin capability declarations now reject blank capability keys and forbidden direct database sandbox capabilities such as `sandbox.database`, `sandbox.db`, and `sandbox.spacetimedb`, keeping reducer-controlled database mutation out of sidecar capability rows. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-bundles.test.ts` |
| Phase 06 | SpacetimeDB plugin capability declarations now reject non-empty constraint payloads unless they are JSON objects, keeping malformed signed capability constraints from becoming authoritative control-plane rows. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-bundles.test.ts` |
| Phase 06 | SpacetimeDB plugin capability declarations now validate known constraint fields, rejecting malformed payload limits and actor principal allow-lists before signed capability metadata becomes authoritative. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-bundles.test.ts` |
| Phase 06 | Runtime deployment flows can download registered plugin artifact URLs through an HTTP artifact fetcher before hash/signature verification, including admin approval requests from artifact metadata and browser panel artifact deployment requests. | `src/core/plugin-deployment.ts`, `src/admin/service.ts`, `src/admin/http-api.ts`, `admin-panel/`, `tests/plugin-deployment.test.ts`, `tests/admin-deployment-http-api.test.ts`, `tests/admin-panel-assets.test.ts` |
| Phase 06 | The built-in HTTP artifact fetcher now fails closed for plain `http://` plugin artifact URLs and only downloads artifacts over HTTPS before hash/signature verification. | `src/core/plugin-deployment.ts`, `tests/plugin-deployment.test.ts` |
| Phase 06 | Direct artifact deployment now preflights bundle ownership and revoked signer state before downloading bundle bytes, matching artifact-backed approval requests and avoiding unnecessary fetches for metadata-invalid deployments. | `src/core/plugin-deployment.ts`, `tests/plugin-deployment.test.ts` |
| Phase 06 | Plugin bundle metadata, declared capabilities, and deployments are persisted through SpacetimeDB tables/reducers with bundle writes guarded against unknown plugin ids, capability writes guarded against plugin/bundle ownership mismatches, deployment writes guarded against unknown plugins, unknown servers, unknown bundles, and plugin/bundle mismatches, adapter subscriptions and live cache, admin write-through, admin deployment read snapshots, request/approval/failed/kill/rollback audit write-through, signer revocation routes, and browser panel controls/read refresh. | `spacetimedb/src/lib.rs`, `src/spacetime/adapter.ts`, `src/admin/service.ts`, `src/admin/http-api.ts`, `src/core/plugin-deployment.ts`, `admin-panel/`, `tests/spacetimedb-plugin-bundles.test.ts`, `tests/spacetimedb-plugin-bundle-adapter.test.ts`, `tests/admin-spacetime-write-through.test.ts`, `tests/admin-deployment-http-api.test.ts`, `tests/admin-panel-assets.test.ts` |
| Phase 06 | Admin deployment capability reads can be scoped to a target server through the service facade, HTTP API, SpacetimeDB live cache, and browser panel controls, preventing another server's active plugin deployment from being reported as locally capable. | `src/admin/service.ts`, `src/admin/http-api.ts`, `admin-panel/`, `tests/admin-deployment-service.test.ts`, `tests/admin-deployment-http-api.test.ts`, `tests/admin-panel-assets.test.ts` |
| Phase 06 | Signer revocation now kills already-active deployments and cancels pending deployments signed by the revoked signer, records deployment kill audit entries, clears queued bundle bytes, removes their capability surface immediately, mirrors killed deployment rows through admin SpacetimeDB write-through, and returns affected deployments from the admin HTTP route. | `src/core/plugin-deployment.ts`, `src/admin/service.ts`, `src/admin/http-api.ts`, `tests/plugin-lifecycle-controls.test.ts`, `tests/admin-spacetime-write-through.test.ts`, `tests/admin-deployment-http-api.test.ts` |
| Phase 06 | Deployment signer revocation now requires an operator reason, propagates that reason into killed deployment error messages and lifecycle audit metadata, and carries it through admin HTTP/panel controls plus SpacetimeDB audit write-through. | `src/core/plugin-deployment.ts`, `src/admin/service.ts`, `src/admin/http-api.ts`, `admin-panel/`, `tests/plugin-lifecycle-controls.test.ts`, `tests/admin-spacetime-write-through.test.ts`, `tests/admin-deployment-http-api.test.ts`, `tests/admin-panel-assets.test.ts` |
| Phase 06 | Local deployment signer revocation now rejects blank signer ids before signer lookup, deployment mutation, queued bundle cleanup, or lifecycle audit writes. | `src/core/plugin-deployment.ts`, `tests/plugin-lifecycle-controls.test.ts` |
| Phase 06 | Revoked signers now fail closed for future bundle registration and pending deployment requests, including artifact-backed request paths that reject before downloading bundle bytes, with admin HTTP routes returning the same signer-revoked error at request time. | `src/core/plugin-deployment.ts`, `tests/plugin-lifecycle-controls.test.ts`, `tests/plugin-deployment.test.ts`, `tests/admin-deployment-http-api.test.ts` |
| Phase 06 | Plugin kill switch now cancels pending deployments as well as active deployments, clearing pending bundle bytes so disabled or compromised plugins cannot later approve queued rollouts. | `src/core/plugin-deployment.ts`, `tests/plugin-lifecycle-controls.test.ts` |
| Phase 06 | Sidecar supervisor starts active deployments through a sandbox driver, records startup failures, stop failures, and heartbeats, fails stale instances, stops killed deployments, mirrors sandbox events through SpacetimeDB reducers with plugin/server existence guards and dedupe, and exposes server-scoped sandbox diagnostics in deployment reads. | `src/core/plugin-sidecar.ts`, `src/admin/service.ts`, `src/spacetime/adapter.ts`, `spacetimedb/src/lib.rs`, `tests/plugin-sidecar-supervisor.test.ts`, `tests/spacetimedb-sandbox-events.test.ts`, `tests/spacetimedb-sandbox-event-adapter.test.ts`, `tests/admin-spacetime-write-through.test.ts`, `tests/admin-deployment-http-api.test.ts` |
| Phase 06 | SpacetimeDB sandbox event writes now reject blank event ids, plugin ids, server ids, event types, and payload hashes before plugin/server lookup or diagnostic row insertion. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-sandbox-events.test.ts` |
| Phase 06 | In-memory admin deployment snapshots now retain deduped mirrored sandbox events, so local development reads expose sidecar diagnostics even before a SpacetimeDB cache is connected. | `src/admin/service.ts`, `tests/admin-deployment-service.test.ts` |
| Phase 06 | FiveM runtime connector sync passes now read deployment diagnostics, filter them to the target server, suppress duplicate snapshots, emit trusted `sdb_runtime:syncDeployments` events, and the `sdb_runtime` resource caches deployment/sandbox diagnostics behind a `GetDeployments` export. | `src/connectors/fivem-runtime.ts`, `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/fivem-runtime-connector.test.ts`, `tests/fivem-resource.test.ts` |
| Phase 06 | FiveM runtime connector deployment sync passes can now reconcile the local sidecar supervisor from server-scoped deployment rows and bundle metadata on every pass, even when diagnostics emission is suppressed as unchanged. | `src/connectors/fivem-runtime.ts`, `tests/fivem-runtime-connector.test.ts` |
| Phase 06 | Admin connector bootstrap can wire a sidecar supervisor into the FiveM runtime sync connector, allowing active server-scoped deployments from the control plane to start local sandbox sidecars through the normal sync path. | `src/admin/bootstrap.ts`, `tests/admin-bootstrap.test.ts` |
| Phase 06 | FiveM `sdb_runtime` trusted cache sync events for permissions, config, health, menu trees, and deployment diagnostics are now server-only, rejecting client-originated cache mutation attempts. | `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/fivem-resource.test.ts` |
| Phase 06 | Stopped or failed sidecar instances no longer block a fresh sandbox start for the same active deployment because the supervisor clears stale deployment-instance indexes during restart and stop lifecycle paths. | `src/core/plugin-sidecar.ts`, `tests/plugin-sidecar-supervisor.test.ts` |
| Phase 06 | Sidecar reconciliation treats the latest active deployment per plugin/server as desired state, starting the newest bundle and stopping superseded active sidecars from older deployment history rows. | `src/core/plugin-sidecar.ts`, `tests/plugin-sidecar-supervisor.test.ts` |
| Phase 06 | Sidecar-requested runtime actions pass through a broker that checks active plugin capabilities and payload schemas before dispatching through the runtime bridge, and broker-level capability/schema denials are written to the runtime audit log. | `src/core/plugin-action-broker.ts`, `src/runtime/bridge.ts`, `src/core/runtime.ts`, `tests/plugin-action-broker.test.ts` |
| Phase 06 | Sidecar-requested runtime action capability checks are scoped to the target runtime server, preventing a plugin deployment active on one server from authorizing actions through another server's bridge. | `src/core/plugin-action-broker.ts`, `src/core/plugin-deployment.ts`, `src/runtime/bridge.ts`, `tests/plugin-action-broker.test.ts`, `tests/plugin-deployment.test.ts` |
| Phase 06 | Sidecar-requested runtime actions enforce signed bundle capability payload limits before dispatch and fail closed on malformed limit declarations, with denied attempts audited through the runtime bridge. | `src/core/plugin-action-broker.ts`, `tests/plugin-action-broker.test.ts` |
| Phase 06 | Sidecar-requested runtime action payload limits now fail closed when limited fields are missing, non-numeric, or non-finite, preventing malformed payloads from bypassing signed capability bounds and reaching runtime dispatch. | `src/core/plugin-action-broker.ts`, `tests/plugin-action-broker.test.ts` |
| Phase 06 | Sidecar-requested runtime actions can be constrained to explicit actor principals through signed bundle capability metadata, with malformed actor-scope declarations failing closed and denied attempts audited before dispatch. | `src/core/plugin-action-broker.ts`, `tests/plugin-action-broker.test.ts` |
| Phase 06 | Signed bundle registration and sidecar action dispatch now reject whitespace-only actor principal entries in capability actor allow-lists before malformed actor-scope metadata can authorize or deny plugin actions ambiguously. | `src/core/plugin-deployment.ts`, `src/core/plugin-action-broker.ts`, `tests/plugin-deployment.test.ts`, `tests/plugin-action-broker.test.ts` |
| Phase 06 | Sidecar-requested runtime actions now reject blank plugin ids, actor principal ids, and action ids before capability lookup, audit writes, or runtime dispatch. | `src/core/plugin-action-broker.ts`, `tests/plugin-action-broker.test.ts` |
| Phase 06 | Sidecar action broker construction now rejects blank runtime action ids and blank capability names in action-capability mappings before accepting plugin action requests. | `src/core/plugin-action-broker.ts`, `tests/plugin-action-broker.test.ts` |
| Phase 06 | Sidecar action broker construction now rejects blank runtime action ids in payload-schema mappings and clones accepted schemas before enforcing sidecar action payload validation. | `src/core/plugin-action-broker.ts`, `tests/plugin-action-broker.test.ts` |
| Phase 06 | Sidecar hook audit writes now reject blank actor principals, plugin ids, hook names, hook ids, and capability names before runtime audit rows are written. | `src/core/plugin-action-broker.ts`, `tests/plugin-action-broker.test.ts` |
| Phase 06 | Sidecar hook audit writes now reject invalid audit statuses at the plugin action broker boundary before malformed rows can reach the generic runtime audit writer. | `src/core/plugin-action-broker.ts`, `tests/plugin-action-broker.test.ts` |
| Phase 06 | SpacetimeDB plugin bundle registration now rejects bundle hashes that do not declare an explicit `sha256:<digest>` algorithm prefix before plugin lookup or signed bundle metadata insertion. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-bundles.test.ts` |
| Phase 06 | Sidecar hook events can be dispatched to running plugin sidecars after hook payload validation and server-scoped hook capability checks, and any returned runtime actions are executed through the existing action broker so action capabilities, payload schemas, actor constraints, permission checks, and audit logging still apply. | `src/core/plugin-sidecar-hooks.ts`, `src/core/plugin-sidecar.ts`, `src/core/plugin-action-broker.ts`, `tests/plugin-sidecar-hook-broker.test.ts` |
| Phase 06 | Sidecar hook dispatch now binds to the current active deployment's running sidecar instance, preventing stale still-running sidecars from receiving hooks after an upgrade, rollback, or superseded deployment. | `src/core/plugin-sidecar-hooks.ts`, `tests/plugin-sidecar-hook-broker.test.ts` |
| Phase 06 | Sidecar hook action responses are bound to the original hook actor principal, preventing plugin code from escalating returned runtime actions to a different actor while preserving action-broker denial audit records. | `src/core/plugin-sidecar-hooks.ts`, `tests/plugin-sidecar-hook-broker.test.ts` |
| Phase 06 | Sidecar hook action fanout is bounded per hook response before any returned actions execute, preventing a single plugin hook event from flooding runtime dispatch while preserving partial-result reporting for later action denials. | `src/core/plugin-sidecar-hooks.ts`, `tests/plugin-sidecar-hook-broker.test.ts` |
| Phase 06 | Sidecar hook-level audit now marks returned-action capability denials, actor-scope denials, malformed capability constraints, and missing action capability mappings as denied instead of generic failures, keeping hook audit status aligned with action-broker denial records. | `src/core/plugin-sidecar-hooks.ts`, `tests/plugin-sidecar-hook-broker.test.ts` |
| Phase 06 | Sidecar hook broker registration and dispatch now reject blank hook ids, plugin ids, hook names, capabilities, handler refs, non-finite priorities, blank dispatch hook names, and blank actor principals before plugin code can be reached. | `src/core/plugin-sidecar-hooks.ts`, `tests/plugin-sidecar-hook-broker.test.ts` |
| Phase 06 | Sidecar hook fanout configuration now rejects negative, fractional, and non-finite limits at broker construction time, keeping hook action flood controls explicit before any plugin dispatch path is available. | `src/core/plugin-sidecar-hooks.ts`, `tests/plugin-sidecar-hook-broker.test.ts` |
| Phase 06 | Sidecar hook responses are validated at the sandbox supervisor boundary so malformed action lists fail closed, record `sidecar.hook_failed`, and never reach runtime action dispatch. | `src/core/plugin-sidecar.ts`, `tests/plugin-sidecar-supervisor.test.ts` |
| Phase 06 | Sidecar hook action responses now reject whitespace-only action ids and actor principal ids at the sandbox supervisor boundary, recording `sidecar.hook_failed` before any returned runtime action can dispatch. | `src/core/plugin-sidecar.ts`, `tests/plugin-sidecar-supervisor.test.ts` |
| Phase 06 | Sidecar hook dispatch writes hook-level runtime audit records for success, failure, and capability denial, including hooks that return no actions or fail before sidecar dispatch. | `src/core/plugin-sidecar-hooks.ts`, `src/core/plugin-action-broker.ts`, `src/runtime/bridge.ts`, `tests/plugin-sidecar-hook-broker.test.ts` |
| Phase 06 | Runtime audit entries, including sidecar hook audit rows, can be mirrored through the admin service into SpacetimeDB audit reducers with audit-id dedupe. | `src/admin/service.ts`, `tests/admin-spacetime-write-through.test.ts` |
| Phase 06 | Admin HTTP exposes a server-scoped runtime audit mirror route that waits for SpacetimeDB write-through, allowing operator/runtime loops to persist sidecar hook audit rows on demand. | `src/admin/http-api.ts`, `tests/admin-http-api.test.ts` |
| Phase 06 | FiveM runtime connector full sync passes call the server-scoped runtime audit mirror route, so local runtime and sidecar hook audit rows are persisted during the normal control-plane sync loop. | `src/connectors/fivem-runtime.ts`, `tests/fivem-runtime-connector.test.ts` |
| Phase 06 | Runtime action envelopes support HMAC signing, signature verification hooks, server-scoped nonce replay protection, server-scoped idempotency conflict detection, and SpacetimeDB reducer replay guards. | `src/core/actions.ts`, `src/core/runtime.ts`, `spacetimedb/src/lib.rs`, `tests/action-envelope.test.ts`, `tests/runtime-control-plane.test.ts`, `tests/spacetimedb-module.test.ts` |
| Phase 06 | Runtime bridge menu/action dispatch can sign submitted action envelopes with a server action secret before calling the control plane, allowing deployments that require action signature verification while preserving unsigned local-development mode. | `src/runtime/bridge.ts`, `src/core/actions.ts`, `tests/runtime-bridge.test.ts`, `tests/action-envelope.test.ts` |
| Phase 06 | Active plugin deployments can be marked failed by the runtime after health failures, clearing pending bytes, writing failed-deployment audit metadata, restoring the previous active signed bundle/version when available, and removing capabilities that only existed on the failed bundle. | `src/core/plugin-deployment.ts`, `tests/plugin-deployment.test.ts` |
| Phase 06 | Manual rollback and health-failure rollback now skip deployment history backed by revoked bundle versions, preventing a compromised old bundle from being restored after its bundle record has been revoked. | `src/core/plugin-deployment.ts`, `tests/plugin-deployment.test.ts` |
| Phase 06 | Manual rollback and health-failure rollback now skip deployment history signed by revoked signers, preventing previously superseded plugin code from being restored after signer compromise. | `src/core/plugin-deployment.ts`, `tests/plugin-deployment.test.ts` |
| Phase 06 | Pending plugin deployments can be marked failed or cancelled without creating duplicate rollback deployments, while active deployment failures still restore the previous signed bundle when available. | `src/core/plugin-deployment.ts`, `tests/plugin-deployment.test.ts` |
| Phase 06 | Runtime or sidecar health failures can be reported through the admin service and `/deployments/:id/fail` route, mirroring failed deployment rows, restored rollback rows, and deployment audit logs while updating the server-scoped capability surface. | `src/admin/service.ts`, `src/admin/http-api.ts`, `tests/admin-deployment-service.test.ts`, `tests/admin-deployment-http-api.test.ts`, `tests/admin-spacetime-write-through.test.ts` |
| Phase 06 | Stale sidecar heartbeat failures can be consumed by the admin service as deployment failures, using the failed sidecar deployment id to restore the previous active bundle and remove capabilities introduced by the unhealthy deployment. | `src/core/plugin-sidecar.ts`, `src/admin/service.ts`, `src/core/plugin-deployment.ts`, `tests/admin-deployment-service.test.ts`, `tests/plugin-sidecar-supervisor.test.ts` |
| Phase 06 | Stale sidecar failure batches skip deployments that are already failed, killed, or otherwise non-actionable while still allowing sandbox event mirroring, preventing one obsolete sidecar from blocking other failure handling. | `src/admin/service.ts`, `tests/admin-deployment-service.test.ts` |
| Phase 06 | Stale sidecar failure orchestration can mirror sandbox events, failed deployment rows, restored rollback rows, and deployment audit logs to SpacetimeDB in one deduped admin service path. | `src/admin/service.ts`, `src/core/plugin-sidecar.ts`, `tests/admin-spacetime-write-through.test.ts` |
| Phase 06 | Browser deployment controls expose the tested deployment failure route so operators can mark unhealthy deployments failed from the web panel using the existing actor/reason workflow. | `admin-panel/`, `tests/admin-panel-assets.test.ts`, `tests/admin-deployment-http-api.test.ts` |
| Phase 06 | Sidecar startup fails closed for unapproved sandbox capability requests such as filesystem, OS, or network access, records failed sandbox startup events before driver execution, and passes explicit allowed/requested sandbox policy to the sandbox driver when exceptions are approved. | `src/core/plugin-sidecar.ts`, `tests/plugin-sidecar-supervisor.test.ts` |
| Phase 06 | Pending deployment approval fails closed when a signed bundle requests unapproved sandbox capabilities, recording failed-deployment audit metadata before activation while allowing explicitly approved sandbox exceptions after hash/signature verification. | `src/core/plugin-deployment.ts`, `tests/plugin-deployment.test.ts`, `tests/plugin-sidecar-supervisor.test.ts` |
| Phase 06 | Direct database sandbox capabilities such as `sandbox.database.write` are now forbidden even if accidentally listed as approved, blocking deployment activation and sidecar startup before plugin code can bypass reducer-controlled database mutation. | `src/core/plugin-deployment.ts`, `src/core/plugin-sidecar.ts`, `tests/plugin-deployment.test.ts`, `tests/plugin-sidecar-supervisor.test.ts` |
| Phase 06 | Admin connector bootstrap and CLI startup can pass approved sandbox capability exceptions into the deployment manager through `approvedSandboxCapabilities` and `SDB_PLUGIN_APPROVED_SANDBOX_CAPABILITIES`. | `src/admin/bootstrap.ts`, `src/admin/main.ts`, `tests/admin-bootstrap.test.ts` |
| Phase 06 | Direct deployment and artifact deployment activation paths share the same hash/signature and sandbox capability approval checks as pending approval, preventing direct activation from bypassing sandbox policy. | `src/core/plugin-deployment.ts`, `tests/plugin-deployment.test.ts` |
| Phase 06 | Local direct and pending deployment requests now reject blank plugin, bundle, server, and requester identities before deployment rows or pending bundle bytes are created. | `src/core/plugin-deployment.ts`, `tests/plugin-deployment.test.ts` |
| Phase 06 | Local deployment approval and failure paths now reject blank approver, failure actor, and failure reason fields before pending or active deployments mutate, preventing blank lifecycle audit actors and empty failure reasons. | `src/core/plugin-deployment.ts`, `tests/plugin-deployment.test.ts` |
| Phase 06 | Direct activation failures from hash/signature or sandbox-policy verification emit explicit `plugin.deployment_failed` audit metadata with the failure reason instead of only recording a generic direct deployment event. | `src/core/plugin-deployment.ts`, `tests/plugin-deployment.test.ts` |
| Phase 06 | Deployment failure lifecycle audit rows now carry failed status through direct activation failure, pending approval failure, runtime health failure, rollback orchestration, and SpacetimeDB write-through. | `src/core/plugin-deployment.ts`, `tests/plugin-deployment.test.ts`, `tests/admin-spacetime-write-through.test.ts` |
| Phase 06 | Successful direct activation and approval now enforce a single active deployment per plugin/server by moving the previous active row into rollback history, while manual rollback and health-failure restore still reactivate the previous signed bundle and admin approval mirrors superseded rows through SpacetimeDB. | `src/core/plugin-deployment.ts`, `src/admin/service.ts`, `tests/plugin-deployment.test.ts`, `tests/admin-spacetime-write-through.test.ts`, `tests/admin-deployment-service.test.ts`, `tests/admin-deployment-http-api.test.ts` |
| Phase 06 | SpacetimeDB active deployment writes now also enforce a single active deployment per plugin/server by rolling back any previous active row in the reducer transaction, preventing direct control-plane writes from bypassing deployment superseding. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-bundles.test.ts` |
| Phase 06 | SpacetimeDB deployment writes now reject blank deployment, plugin, bundle, and server ids before control-plane lookups, keeping deployment rows tied to explicit plugin/server ownership. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-bundles.test.ts` |
| Phase 06 | SpacetimeDB deployment upserts now reject attempts to move an existing deployment id across plugin, bundle, or server ownership, preserving immutable deployment identity while still allowing lifecycle status updates. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-bundles.test.ts` |
| Phase 06 | SpacetimeDB active and pending deployment writes now require desired and active version fields to match the referenced bundle version, preventing direct reducer calls from publishing misleading rollout state. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-bundles.test.ts` |
| Phase 06 | SpacetimeDB pending deployment writes now require the referenced bundle to still be registered, matching local approval-request preflights and preventing revoked bundles from re-entering the rollout queue through direct reducer calls. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-bundles.test.ts` |
| Phase 06 | SpacetimeDB deployment writes now reject lifecycle/error mismatches, keeping pending, active, and rolled-back rows free of error text while requiring failed and killed rows to carry an explicit failure reason. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-bundles.test.ts` |
| Phase 06 | The sidecar supervisor now fails closed before driver startup for non-sidecar runtime types, recording a failed sandbox startup event instead of launching `wasm` bundles through the sidecar execution path. | `src/core/plugin-sidecar.ts`, `tests/plugin-sidecar-supervisor.test.ts` |
| Phase 06 | Sidecar hook dispatch now has a supervisor-level timeout that fails closed, records `sidecar.hook_failed`, and leaves the running instance marked with the timeout error when a sandbox driver hangs. | `src/core/plugin-sidecar.ts`, `tests/plugin-sidecar-supervisor.test.ts` |
| Phase 06 | Sidecar supervisor timeout settings now fail fast unless heartbeat and hook dispatch timeouts are positive finite numbers, preventing accidental configuration that disables stale-instance or hung-hook protection. | `src/core/plugin-sidecar.ts`, `tests/plugin-sidecar-supervisor.test.ts` |
| Phase 06 | Plugin bundle signature verification now binds the signed payload to bundle id, plugin id, version, runtime type, signer id, bundle hash, and declared capabilities, preventing metadata-only capability expansion from activating after signing. | `src/core/plugin-deployment.ts`, `tests/plugin-deployment.test.ts`, `tests/plugin-action-broker.test.ts`, `tests/plugin-sidecar-hook-broker.test.ts`, `tests/admin-deployment-service.test.ts`, `tests/admin-spacetime-write-through.test.ts` |
| Phase 06 | Local plugin deployment verification now accepts explicit `sha256:<digest>` bundle hashes, rejects malformed or non-`sha256` algorithm-prefixed hashes at registration, and compares prefixed hashes against downloaded artifact bytes during activation. | `src/core/plugin-deployment.ts`, `tests/plugin-deployment.test.ts` |
| Phase 06 | Operators can now revoke one compromised plugin bundle version without revoking the signer or whole plugin, killing active/pending deployments for that bundle, blocking future use, auditing the revocation, exposing an admin HTTP route, and mirroring the status change through a dedicated SpacetimeDB reducer. | `src/core/plugin-deployment.ts`, `src/admin/service.ts`, `src/admin/http-api.ts`, `src/spacetime/adapter.ts`, `spacetimedb/src/lib.rs`, `tests/plugin-lifecycle-controls.test.ts`, `tests/admin-deployment-http-api.test.ts`, `tests/admin-spacetime-write-through.test.ts`, `tests/spacetimedb-plugin-bundles.test.ts` |
| Phase 06 | SpacetimeDB bundle registration now only creates `registered` bundle rows and rejects blank version, artifact URL, bundle hash, signature, or signer id fields before signed bundle metadata becomes authoritative. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-bundles.test.ts` |
| Phase 06 | SpacetimeDB bundle registration now rejects bundle metadata signed by revoked package signers at reducer time, matching package install trust checks and preventing compromised signers from adding new deployable bundles directly. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-bundles.test.ts` |
| Phase 06 | SpacetimeDB bundle revocation now disables capability rows for that bundle and kills active or pending deployments tied to it inside the reducer, preventing direct revocation calls from leaving revoked code live. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-bundles.test.ts` |
| Phase 06 | SpacetimeDB bundle revocation now requires actor and reason context, rejects blank revocation inputs before bundle mutation, propagates the revocation reason into killed deployment rows, and receives the same actor/reason through admin write-through. | `spacetimedb/src/lib.rs`, `src/spacetime/adapter.ts`, `src/admin/service.ts`, `tests/spacetimedb-plugin-bundles.test.ts`, `tests/admin-spacetime-write-through.test.ts` |
| Phase 06 | Generated SpacetimeDB client reducer ordering is covered for bundle revocation actor/reason arguments, keeping real generated binding calls aligned with the reducer contract used by admin write-through. | `src/spacetime/adapter.ts`, `tests/spacetimedb-generated-client.test.ts` |
| Phase 06 | SpacetimeDB package signer revocation now revokes all non-revoked bundles signed by that signer, disables their capabilities, kills active or pending deployments, and includes bundle-owned plugins in the affected plugin list. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-registry-control-plane.test.ts`, `tests/spacetimedb-plugin-bundles.test.ts` |
| Phase 06 | SpacetimeDB plugin status activation now rejects direct `active` writes when any installed package or registered bundle for that plugin is signed by a revoked signer. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-registry-control-plane.test.ts` |
| Phase 06 | SpacetimeDB plugin lifecycle reducers now disable plugin capability rows when a plugin becomes inactive or its package signer is revoked, preventing direct reducer calls from leaving stale capability grants enabled. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-bundles.test.ts` |
| Phase 06 | SpacetimeDB deployment writes now reject `active` deployments unless the owning plugin is active and the referenced bundle is still registered, preventing direct reducer calls from activating disabled plugins or revoked bundle versions. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-bundles.test.ts` |
| Phase 06 | SpacetimeDB capability writes now reject `enabled` capability rows unless the owning plugin is active and the referenced bundle is still registered, preventing disabled plugins or revoked bundles from regaining action capabilities through direct reducer calls. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-bundles.test.ts` |
| Phase 06 | Plugin registry disable now triggers the deployment kill switch for active or pending deployments of the disabled plugin, removing server-scoped capabilities, auditing the kill, and mirroring killed deployment rows through SpacetimeDB write-through. | `src/admin/service.ts`, `tests/admin-deployment-service.test.ts`, `tests/admin-spacetime-write-through.test.ts` |
| Phase 06 | Plugin kill switch now writes per-deployment `plugin.deployment_killed` audit rows with kill reasons before the aggregate kill-switch audit, and admin write-through mirrors those rows to SpacetimeDB for full deployment kill traceability. | `src/core/plugin-deployment.ts`, `tests/plugin-lifecycle-controls.test.ts`, `tests/admin-spacetime-write-through.test.ts` |
| Phase 06 | Local kill switch, signer revocation, and bundle revocation paths now reject blank lifecycle actors and revocation/kill reasons before mutating deployments, bundle trust, or lifecycle audit rows. | `src/core/plugin-deployment.ts`, `tests/plugin-lifecycle-controls.test.ts` |
| Phase 05 | Plugin config writes now require the plugin config surface to be active before validation/write-through, so installed or disabled plugins cannot accept server config values until intentionally enabled. | `src/core/plugins.ts`, `tests/plugin-registry.test.ts`, `tests/admin-plugin-registry-control-plane.test.ts` |
| Phase 05 | Local plugin enable, disable, and uninstall lifecycle paths now reject blank plugin ids before status mutation, dependency cascade handling, or lifecycle audit writes. | `src/core/plugins.ts`, `tests/plugin-registry.test.ts` |
| Phase 05 | SpacetimeDB plugin status and uninstall reducers now reject blank plugin ids before direct status lookup, row deletion, or cascading plugin-owned row cleanup. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-registry-control-plane.test.ts` |
| Phase 05 | SpacetimeDB plugin config writes now also require the target plugin to be active at reducer time, preventing direct reducer calls from bypassing the admin/plugin-registry inactive config guard. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-registry-control-plane.test.ts` |
| Phase 05 | SpacetimeDB plugin config writes now reject blank plugin ids, server ids, and config keys, and require the target server to exist before server-scoped config rows are upserted. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-registry-control-plane.test.ts` |
| Phase 05 | SpacetimeDB manifest registration now rejects writes for unknown plugins, preventing orphaned manifest rows from becoming authoritative plugin metadata. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-registry-control-plane.test.ts` |
| Phase 05 | SpacetimeDB plugin manifest registration now rejects blank plugin ids before manifest JSON validation, plugin lookup, or authoritative manifest upsert. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-registry-control-plane.test.ts` |
| Phase 05 | Plugin entity ids are now bound to their original plugin namespace and entity type in both the local JSON extension store and SpacetimeDB reducer, preventing a record from being moved across schema namespaces during upsert. | `src/core/plugin-data.ts`, `spacetimedb/src/lib.rs`, `tests/plugin-data-store.test.ts`, `tests/spacetimedb-plugin-data-module.test.ts` |
| Phase 05 | Plugin manifest config schemas and approved entity schemas are now recursively validated at install time, rejecting invalid schema types and malformed nested property/item declarations before they can enter the registry. | `src/core/plugins.ts`, `tests/plugin-registry.test.ts` |
| Phase 05 | Plugin manifest capability declarations now reject empty and duplicate required/provided capability keys at install time, keeping dependency/capability gating deterministic. | `src/core/plugins.ts`, `tests/plugin-registry.test.ts`, `tests/plugin-dependencies.test.ts` |
| Phase 05 | Plugin manifest runtime primitives now reject empty menu/command/FiveM action identifiers, empty panel routes, malformed command payload schemas, and empty command aliases before they can become active menu or command rows. | `src/core/plugins.ts`, `tests/plugin-registry.test.ts`, `tests/plugin-dependencies.test.ts` |
| Phase 05 | Plugin schema migration plans are now validated as a bounded safe DSL at manifest install time, rejecting unsupported direct-mutation steps, mismatched entity targets, and malformed property schemas before approved schemas enter the registry. | `src/core/plugins.ts`, `tests/plugin-registry.test.ts` |
| Phase 05 | Admin schema registration write-through and the SpacetimeDB `register_plugin_schema` reducer now enforce schema JSON and safe migration-plan validation, rejecting unsupported direct mutation plans before control-plane rows are persisted. | `src/core/plugins.ts`, `src/admin/service.ts`, `spacetimedb/src/lib.rs`, `tests/admin-plugin-data-write-through.test.ts`, `tests/spacetimedb-plugin-data-module.test.ts` |
| Phase 05 | SpacetimeDB plugin schema registration now rejects blank plugin ids and entity types before plugin lookup or schema id creation, preserving explicit plugin data namespaces. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-data-module.test.ts` |
| Phase 05 | Admin plugin entity write-through now validates `dataJson` against the active registered plugin schema, using schemas registered through admin or already present in the SpacetimeDB live cache before reducer mutation. | `src/admin/service.ts`, `tests/admin-plugin-data-write-through.test.ts` |
| Phase 05 | SpacetimeDB plugin entity upserts now validate object-shaped `data_json` against the latest active plugin schema row before mutation, enforcing required fields and simple property types for direct reducer calls. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-data-module.test.ts` |
| Phase 05 | SpacetimeDB plugin entity upserts now reject blank entity ids, plugin ids, entity types, and owner ids before active-plugin lookup, schema lookup, or row mutation. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-data-module.test.ts` |
| Phase 05 | SpacetimeDB plugin data reducers now reject schema registration for unknown plugins and reject entity mutations unless the owning plugin is active, preventing direct reducer calls from mutating disabled plugin namespaces. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-data-module.test.ts` |
| Phase 05 | SpacetimeDB gameplay primitive reducers now reject item, job, vehicle, and location registration unless the owning plugin is active, preventing disabled plugins from adding new gameplay primitives through direct reducer calls. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-gameplay-semantics.test.ts` |
| Phase 05 | Plugin hook declarations now carry a validated handler type (`action`, `reducer`, or `sidecar`) through manifests, active registry reads, SpacetimeDB rows, adapter live cache, generated binding types, and reducer-side validation. | `src/core/plugins.ts`, `src/spacetime/adapter.ts`, `src/spacetime/module_bindings/index.ts`, `spacetimedb/src/lib.rs`, `tests/plugin-hook-manifest.test.ts`, `tests/spacetimedb-gameplay-adapter.test.ts`, `tests/spacetimedb-gameplay-semantics.test.ts` |
| Phase 05 | Admin plugin enable write-through now mirrors active plugin hook rows, including handler type, into the SpacetimeDB `plugin_hooks` reducer before status sync so runtime subscriptions can rebuild hook registries from authoritative rows. | `src/admin/service.ts`, `src/spacetime/adapter.ts`, `tests/admin-plugin-registry-control-plane.test.ts` |
| Phase 05 | Plugin disable write-through now deactivates mirrored SpacetimeDB hook rows before status sync, preventing runtime subscriptions from dispatching stale plugin hooks after a plugin is disabled. | `src/admin/service.ts`, `src/spacetime/adapter.ts`, `src/spacetime/module_bindings/index.ts`, `spacetimedb/src/lib.rs`, `tests/admin-plugin-registry-control-plane.test.ts`, `tests/spacetimedb-gameplay-semantics.test.ts`, `tests/spacetimedb-expanded-module.test.ts` |
| Phase 05 | SpacetimeDB plugin status and hook reducers now fail closed at the reducer boundary by disabling hook rows when a plugin becomes inactive and rejecting direct hook enablement unless the plugin is active. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-gameplay-semantics.test.ts` |
| Phase 05 | SpacetimeDB hook registration now also requires the owning plugin to be active before inserting an enabled hook row, preventing disabled plugins from rejoining hook dispatch through direct reducer calls. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-gameplay-semantics.test.ts` |
| Phase 05 | SpacetimeDB hook enablement now rejects blank plugin ids before plugin lookup or authoritative hook row mutation. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-gameplay-semantics.test.ts` |
| Phase 05 | SpacetimeDB plugin runtime instance writes now reject unknown server ids and require the plugin to be active before reporting a `loaded` runtime instance, while still allowing unloaded/disabled/failed cleanup states. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-registry-control-plane.test.ts` |
| Phase 05 | SpacetimeDB plugin runtime instance writes now reject blank plugin/server identity fields and lifecycle/error mismatches before authoritative runtime instance rows are upserted. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-registry-control-plane.test.ts` |
| Phase 05 | SpacetimeDB plugin uninstall now removes plugin-owned bundles, capabilities, deployments, runtime instances, config values, schemas, extension entities, hooks, menu surfaces, permissions, and dependent grants/ACE mirrors/policy constraints instead of leaving orphaned authoritative rows. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-plugin-registry-control-plane.test.ts` |
| Phase 02 | Discord role synchronization now has an admin control-plane route that plans role-derived principals/edges, applies local permission-store changes, mirrors principal/edge mutations through SpacetimeDB reducers, and writes a `discord.role_sync` audit row. | `src/connectors/discord.ts`, `src/admin/service.ts`, `src/admin/http-api.ts`, `tests/discord-connector.test.ts`, `tests/admin-http-api.test.ts`, `tests/admin-spacetime-write-through.test.ts` |
| Phase 02 | Browser admin permissions controls now expose Discord role synchronization, accepting guild/member snapshots and role mappings, calling the tested connector route, and rendering the sync plan for operator review. | `admin-panel/index.html`, `admin-panel/app.js`, `tests/admin-panel-assets.test.ts` |
| Phase 04 | Core invoice issuance now accepts optional idempotency keys, returning the original invoice on identical retries and rejecting reused keys with changed invoice payloads before creating duplicates. | `src/core/economy.ts`, `tests/economy-invoices.test.ts` |
| Phase 04 | SpacetimeDB invoice issuance now carries an idempotency key through reducer calls and invoice rows, accepting identical reducer retries and rejecting reused keys with changed invoice payloads before inserting duplicate invoices. | `spacetimedb/src/lib.rs`, `src/spacetime/adapter.ts`, `src/spacetime/module_bindings/index.ts`, `tests/spacetimedb-economy-semantics.test.ts`, `tests/spacetimedb-economy-adapter.test.ts`, `tests/spacetimedb-generated-client.test.ts` |
| Phase 04 | SpacetimeDB invoice issuance now validates issuer/payer account existence, active status, and currency before persisting invoice rows, matching the core ledger's issue-invoice invariants. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-economy-semantics.test.ts` |
| Phase 04 | SpacetimeDB economy reducers now require non-expired actor permission grants before transfer, single-account, invoice, and void mutations, with deny grants overriding allows and admin adjustment mapped to the public permission key. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-economy-semantics.test.ts` |
| Phase 04 | SpacetimeDB economy reducer permission checks now traverse non-expired principal edges so inherited group grants authorize reducer calls while inherited deny grants still fail closed. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-economy-semantics.test.ts` |
| Phase 04 | Economy mutations now reject blank or whitespace-only reasons before balances, ledger rows, invoices, or idempotency records are written, and SpacetimeDB reducer helpers enforce the same reason invariant for direct reducer calls. | `src/core/economy.ts`, `spacetimedb/src/lib.rs`, `tests/economy-ledger.test.ts`, `tests/spacetimedb-economy-semantics.test.ts` |
| Phase 04 | Admin economy transfer, deposit, withdrawal, balance adjustment, and void write-through paths now validate positive integer amounts, non-empty reasons, and adjustment direction before calling SpacetimeDB reducers. | `src/core/economy.ts`, `src/admin/service.ts`, `tests/admin-economy-write-through.test.ts` |
| Phase 04 | Admin invoice issue and payment routes now write through to SpacetimeDB reducers, including due-date parsing and issue-invoice amount/reason validation at the HTTP/service boundary. | `src/admin/service.ts`, `src/admin/http-api.ts`, `tests/admin-economy-write-through.test.ts` |
| Phase 04 | Browser admin economy controls now expose invoice issue and payment workflows that call the tested invoice HTTP routes from the dashboard. | `admin-panel/`, `tests/admin-panel-assets.test.ts` |
| Phase 04 | Admin service, HTTP routes, and browser economy controls now expose advanced ledger workflows for salaries, player fines, taxes, business payouts, and item buy/sell operations through the existing SpacetimeDB reducers. | `src/admin/service.ts`, `src/admin/http-api.ts`, `admin-panel/`, `tests/admin-economy-write-through.test.ts`, `tests/admin-panel-assets.test.ts` |
| Phase 04 | Core economy mutations now retain audit log entries for successful ledger mutations, keep idempotent retries from duplicating audit rows, and expose those local economy audit rows through admin dashboard and HTTP audit search reads. | `src/core/economy.ts`, `src/admin/service.ts`, `tests/economy-ledger.test.ts`, `tests/admin-service.test.ts`, `tests/admin-http-api.test.ts` |
| Phase 04 | Account statements are now exportable as CSV from the core ledger formatter, admin HTTP statement export route, and browser economy panel. | `src/core/economy.ts`, `src/admin/service.ts`, `src/admin/http-api.ts`, `admin-panel/`, `tests/economy-ledger.test.ts`, `tests/admin-economy-write-through.test.ts`, `tests/admin-panel-assets.test.ts` |
| Phase 04 | Admin economy account creation now validates account owner type and non-negative opening balance before SpacetimeDB write-through, matching the reducer-side account invariants at the browser/API boundary. | `src/core/economy.ts`, `src/admin/service.ts`, `tests/admin-economy-write-through.test.ts` |
| Phase 04 | SpacetimeDB economy limit writes now validate documented max amount and allowed owner-type JSON constraints before persistence, preventing malformed limit rows from silently failing open during ledger reducer enforcement. | `spacetimedb/src/lib.rs`, `tests/spacetimedb-economy-semantics.test.ts` |
| Phase 04 | Admin economy limit write-through now validates documented max amount and allowed owner-type JSON constraints before calling SpacetimeDB reducers, preventing malformed browser/API writes from reaching the control plane. | `src/core/economy.ts`, `src/admin/service.ts`, `tests/admin-economy-write-through.test.ts` |
| Phase 04 | Plugin economy capabilities now enforce documented `requires_on_duty` / `requiresOnDuty` constraints against injected actor state before ledger mutation, and malformed duty constraints fail closed. | `src/core/plugin-economy-api.ts`, `tests/plugin-economy-api.test.ts` |
| Production | Checked-in SpacetimeDB TypeScript binding currency is now a runnable production gate that compares Rust public tables and reducers against the official generated bindings before build/deploy. | `scripts/verify-spacetime-bindings.mjs`, `package.json`, `docs/production-runbook.md`, `tests/spacetimedb-module-bindings.test.ts` |
| Production | SpacetimeDB binding provenance is now tracked beside the binding module, verified by the binding gate, documented in the runbook, and rewritten to `official` metadata when `npm run generate:spacetime-bindings` succeeds with the real CLI. | `src/spacetime/module_bindings/provenance.json`, `scripts/generate-spacetime-bindings.mjs`, `scripts/verify-spacetime-bindings.mjs`, `docs/production-runbook.md`, `tests/spacetimedb-module-bindings.test.ts` |
| Production | Official SpacetimeDB TypeScript bindings are now checked in from `spacetime generate`, normalized for NodeNext imports, backed by the `spacetimedb` npm runtime dependency, and proven through the binding verifier plus TypeScript build. | `src/spacetime/module_bindings/`, `src/spacetime/spacetimedb-generated-shim.d.ts`, `scripts/normalize-spacetime-bindings.mjs`, `package.json`, `package-lock.json`, `tests/spacetimedb-module-bindings.test.ts` |
| Production | Local admin HTTP integration smoke now starts a real localhost listener, preserves query strings through the Node server adapter, and verifies dashboard, config write, action queue, and query-filtered action drain routes. | `src/admin/local-integration-smoke.ts`, `src/admin/http-server.ts`, `tests/admin-local-integration-smoke.test.ts`, `tests/admin-http-server.test.ts` |
| Production | FiveM deployment verification is now a runnable gate covering resource manifests, deployable file layout, NUI assets, QBCore `provide`/dependency behavior, and server-only guards on trusted sync events. | `scripts/verify-fivem-deployment.mjs`, `docs/fivem-deployment-checklist.md`, `resources/[runtime]/sdb_runtime`, `resources/[compat]/qb-core`, `tests/fivem-deployment-verifier.test.ts` |
| Production | A deployable `sdb_runtime_smoke` FXServer smoke resource now validates runtime exports and optional QBCore facade exports from inside FiveM, emits explicit client connect/join markers plus a `client:sdb_runtime_smoke:loaded` marker, re-triggers all client fixture checks after a player joins, and is required by the deployment verifier/checklist/runbook for real-server production validation. | `resources/[test]/sdb_runtime_smoke`, `scripts/verify-fivem-deployment.mjs`, `docs/fivem-deployment-checklist.md`, `docs/production-runbook.md`, `tests/fivem-deployment-verifier.test.ts` |
| Production | FXServer smoke transcripts are now machine-verifiable, rejecting smoke-only fixture files, failed smoke lines, missing FXServer resource lifecycle evidence, missing runtime/QBCore/Qbox/client checks, or client PASS lines without client join/connect evidence before real-server validation can be treated as production evidence; client-required failures distinguish no client join/connect evidence from a connected client whose smoke client did not load or whose client fixture checks did not complete. | `scripts/verify-fxserver-smoke-log.mjs`, `package.json`, `docs/fivem-deployment-checklist.md`, `docs/production-runbook.md`, `tests/fxserver-smoke-log-verifier.test.ts`, `tests/fivem-deployment-verifier.test.ts` |
| Production | Production readiness now has an evidence-gated verifier that requires official SpacetimeDB binding provenance plus a passing real FXServer smoke transcript before a deployment can be declared production-ready, requires the complete runner evidence bundle plus client launcher evidence for client-required readiness, and can validate runner connect-info non-empty bind address, non-empty path, smoke timing integrity, evidence-flag integrity, advertised target consistency across connect-info, client launcher, network/player probes, and socket port, primary and alternate connect port ranges, rejection of unresolved `<server-ip>` advertised hosts, alternate FiveM connect URI syntax plus launcher consistency, alternate network/player probe HTTP 200 evidence, socket probe, player probe sample/max consistency, saved verifier-output section completeness and failure-diagnostic integrity, connect-info path consistency, and UUID-shaped shared smoke run ids across the FXServer transcript plus supplied runner artifacts. | `scripts/verify-production-readiness.mjs`, `package.json`, `docs/production-readiness.md`, `docs/production-runbook.md`, `tests/production-readiness-verifier.test.ts`, `tests/docs-production-readiness.test.ts` |
| Production | Operations runbook verification now pins backup/recovery, deployment snapshot, signer revocation, bundle revocation, plugin kill switch, manual rollback, health-failure handling, and audit review procedures to the tested admin lifecycle routes. | `docs/operations-runbook.md`, `scripts/verify-operations-runbook.mjs`, `src/admin/http-api.ts`, `tests/operations-runbook.test.ts` |
| Production | Qbox/QBX compatibility now has a real `qbx_core` facade resource that provides `qbx_core`, depends on `sdb_runtime`, exposes player, metadata, money, job, gang, shared-data, permission, bucket, usable-item, and playerdata-module surfaces over runtime-owned QBCore-shaped state, and is covered by the deployment verifier. | `resources/[compat]/qbx_core`, `tests/qbox-compat-resource.test.ts`, `scripts/verify-fivem-deployment.mjs` |
| Production | Real compatibility fixture resources now represent third-party QBCore and Qbox scripts by depending on the facades, calling common server/client exports and modules, and emitting machine-verifiable FXServer smoke transcript lines. | `resources/[test]/sdb_qbcore_fixture`, `resources/[test]/sdb_qbox_fixture`, `scripts/verify-fxserver-smoke-log.mjs`, `tests/fxserver-smoke-log-verifier.test.ts` |
| Production | Real FXServer smoke now authenticates with a valid Keymaster key, starts the core runtime and QBCore/Qbox compatibility fixture resources, observes a connected FiveM client, emits client join/load markers, and passes all real server/client QBCore/Qbox smoke transcript checks. | `/tmp/spbox-fxserver-smoke/fxserver-smoke.log`, `docs/core-missing-items.md` |
| Production | Real FXServer smoke now has a repeatable runner that fails missing-license, blank or whitespace-only artifact-url/bind/connect-host, invalid-port, invalid-timing, and client-required unresolved-host preflight before staging, validates generated-evidence inputs even in `--prepare-only`, including client-required connect host, caches the pinned Linux artifact outside the smoke workdir, stages only the core resources and compatibility fixtures, writes the minimal smoke `server.cfg` without persisting `sv_licenseKey`, passes the license key at process launch, keeps FXServer stdin open, supports an optional no-client grace window, requires local `/info.json` reachability and TCP/UDP socket evidence by default, repeats the `fivem://connect/...` URI while waiting for client checks, sends the smoke commands, writes `connect-info.txt` with the connect URI, `connect-launcher.html` path, and detected IPv4 addresses, writes `connect-launcher.html` with a clickable FiveM launch link for the run target, alternate detected operator-facing IPv4 launch links such as Tailscale, and browser-side `/info.json` plus `/players.json` checks, writes the transcript and runner artifacts with a shared smoke run id, writes `fxserver-network-probe.txt` with local, advertised, and alternate `/info.json` reachability results, writes `fxserver-socket-probe.txt` with live listener evidence, writes `fxserver-player-probe.txt` with local, advertised, and alternate `/players.json` reachability, sampled player counts, and max observed player count, requires client-required connect-info plus observed players for client-required readiness when those artifacts are supplied, captures the transcript, saves verifier output to `fxserver-smoke-verification.txt`, and preserves the QBCore/Qbox/client smoke verifier exit behavior. | `scripts/run-fxserver-smoke.mjs`, `package.json`, `docs/core-missing-items.md`, `docs/fivem-deployment-checklist.md`, `docs/production-runbook.md`, `tests/fivem-deployment-verifier.test.ts` |
| Demo | A real QBCore hybrid demo installer now fetches pinned upstream QBCore resources for multicharacter, spawn, apartments, clothing, admin menu, banking, HUD, vehicle shop, garages, and vehicle keys, while SPBox supplies the `qb-core`, `oxmysql`, and `qb-inventory` compatibility facades so those resources run against SPBox/SpacetimeDB instead of MySQL as framework source of truth. | `scripts/install-qbcore-demo-resources.mjs`, `resources/[compat]/qb-core`, `resources/[compat]/oxmysql`, `resources/[compat]/qb-inventory`, `docs/qbcore-demo-server.md`, `tests/qbcore-real-demo.test.ts` |
| Demo | Legacy QBCore/Qbox resources can now be scanned into local SPBox plugin package plans, including FiveM manifest metadata, SQL-derived schemas, install steps, compatibility-facade warnings, and package installer payloads. | `src/core/legacy-resource-import.ts`, `scripts/import-legacy-resource.mjs`, `docs/drop-in-compatibility.md`, `tests/legacy-resource-import.test.ts` |
| Demo | Resource import can now run automatically from the top-level FXServer `server.cfg`: `sdb_runtime` requests `/resources/reconcile` on restart, the admin connector reads `SDB_FXSERVER_CFG` plus `SDB_FXSERVER_RESOURCES_ROOT`, imports only ensured resources, registers SQL-derived schemas, and enables plugins in cfg order. | `src/core/resource-cfg-import.ts`, `src/admin/resource-auto-import.ts`, `resources/[runtime]/sdb_runtime/server/main.lua`, `tests/resource-cfg-import.test.ts`, `tests/admin-http-api.test.ts`, `tests/fivem-resource.test.ts` |
| POC | Optional proof-of-concept gameplay plugins now live outside the core framework in `resources/[poc]/sdb_poc_suite`, with basic client spawning, a custom `F7` NUI panel, server-owned demo economy balances, job selection, vehicle spawn/repair, waypoint teleport, weather/time controls, and noclip. | `resources/[poc]/sdb_poc_suite`, `docs/poc-plugins.md`, `tests/poc-resources.test.ts` |

## Current Verification

Run:

```sh
npm test
npm run verify:spacetime-bindings
npm run verify:fivem-deployment
npm run verify:operations-runbook
npm run build
npm run smoke:admin-local
npm run smoke:production-core
cargo check # from spacetimedb/
```

Expected current result:

```txt
90 test files passed
784 tests passed
43 public SpacetimeDB tables verified
63 SpacetimeDB reducers verified
FiveM deployment resource layout verified
Operations runbook lifecycle routes verified
TypeScript build passed
Local admin HTTP integration smoke passed
Production core smoke passed
Rust SpacetimeDB module cargo check passed
Server-side FXServer production readiness passed with QBCore/Qbox transcript checks, `connect-info.txt`, `fxserver-network-probe.txt`, `fxserver-socket-probe.txt`, local and advertised `/players.json` evidence in `fxserver-player-probe.txt`, and `fxserver-smoke-verification.txt`
Client-required FXServer production readiness passed with smoke run id `df19f99f-970a-431a-b9d6-dd0739cfbf2e`, same-run `connect-info.txt` plus `connect-launcher.html` evidence, browser-side `/info.json` and `/players.json` checks, alternate URI `fivem://connect/100.71.161.17:30120`, `Player count observed: 1`, `Max player count observed: 1`, `Player samples observed: 159`, `playerJoining: 1`, `client:sdb_runtime_smoke:loaded`, verified client smoke checks, and all 17 real server/client QBCore/Qbox smoke checks
```

## Next Required Slices

The remaining finite core list lives in [Missing Core Items](./core-missing-items.md).

CORE-01 through CORE-04 are complete. The missing core list is complete; stop core development and move to real QBCore/Qbox script testing, only fixing reproduced compatibility failures with concrete failing tests.
